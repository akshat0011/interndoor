{
  "_comment": "Edit this file to control what gets watched. Everything else has sane defaults.",
  "companiesFile": "companies.json",
  "_companiesFile_note": "The main watchlist, ~900 companies grouped by sector. Delete whole groups from that file to narrow coverage. Anything added to the inline 'companies' list below is merged on top of it.",
  "_companies_note": "Your own additions go here - they are merged with companiesFile.",
  "companies": [
    {
      "name": "Dream11",
      "aliases": [
        "Dream Sports"
      ]
    },
    {
      "name": "EXL",
      "aliases": [
        "EXL Service",
        "ExlService",
        "EXL Analytics"
      ]
    },
    {
      "name": "GE HealthCare",
      "aliases": [
        "GE Healthcare"
      ]
    },
    {
      "name": "Bybit"
    },
    {
      "name": "CertifyOS"
    },
    {
      "name": "Tractebel",
      "aliases": [
        "Tractebel Engineering",
        "Tractebel India"
      ]
    },
    {
      "name": "1Fi",
      "aliases": [
        "1Finance",
        "OneFi"
      ]
    },
    {
      "name": "Joveo"
    },
    {
      "name": "Quicket Solutions"
    },
    {
      "name": "BSI Financial Services",
      "aliases": [
        "BSI Financial"
      ]
    },
    {
      "name": "HPE",
      "aliases": [
        "Hewlett Packard Enterprise",
        "Hewlett-Packard Enterprise"
      ]
    },
    {
      "name": "GE Vernova"
    },
    {
      "name": "PwC",
      "aliases": [
        "PwC Acceleration Center",
        "PricewaterhouseCoopers"
      ]
    },
    {
      "name": "CoinDCX",
      "aliases": [
        "CoinDCX Careers",
        "Neblio Technologies"
      ]
    },
    {
      "name": "Flam",
      "aliases": [
        "FlamAI",
        "Flam AI"
      ]
    },
    {
      "name": "Janitri"
    },
    {
      "name": "Convin"
    },
    {
      "name": "Constraal"
    },
    {
      "name": "Branch International",
      "aliases": [
        "Branch Personal Finance",
        "Branch Metrics India"
      ]
    },
    {
      "name": "Marmon Technologies India",
      "aliases": [
        "Marmon Technologies",
        "Marmon Holdings",
        "Marmon Group"
      ]
    },
    {
      "name": "Gallagher",
      "aliases": [
        "Arthur J. Gallagher",
        "AJG Global"
      ]
    },
    {
      "name": "SLB",
      "aliases": [
        "Schlumberger",
        "SLB India",
        "Schlumberger Limited"
      ]
    },
    {
      "name": "Criteo"
    },
    {
      "name": "Oceaneering"
    },
    {
      "name": "Monotype"
    },
    {
      "name": "Pearson",
      "aliases": [
        "Pearson India"
      ]
    },
    {
      "name": "Enverus"
    },
    {
      "name": "Livspace"
    },
    {
      "name": "Collegedunia"
    },
    {
      "name": "V-Mart Retail",
      "aliases": [
        "V-Mart"
      ]
    },
    {
      "name": "TeamLease Regtech",
      "aliases": [
        "TeamLease"
      ]
    },
    {
      "name": "Bizlem"
    },
    {
      "name": "Bugsmirror"
    },
    {
      "name": "SmartQ",
      "aliases": [
        "Smart Q"
      ]
    },
    {
      "name": "Nippon Data Systems"
    },
    {
      "name": "Picktime"
    },
    {
      "name": "Aerocraft Engineering"
    },
    {
      "name": "AppVersal"
    },
    {
      "name": "Menstrupedia"
    },
    {
      "name": "MiM-Essay"
    },
    {
      "name": "unbundl"
    },
    {
      "name": "National Technology Centre for Ports, Waterways and Coasts",
      "aliases": [
        "NTCPWC"
      ]
    },
    {
      "name": "SurveySparrow",
      "aliases": [
        "Survey Sparrow"
      ]
    },
    {
      "name": "Zycus"
    },
    {
      "name": "Chegg India",
      "aliases": [
        "Chegg"
      ]
    },
    {
      "name": "NielsenIQ",
      "aliases": [
        "Nielsen IQ",
        "NIQ"
      ]
    },
    {
      "name": "Fanatics"
    },
    {
      "name": "Exabeam",
      "aliases": [
        "LogRhythm"
      ]
    },
    {
      "name": "TUV SUD",
      "aliases": [
        "TÜV SÜD",
        "TUV SUD South Asia",
        "TUV SUD India"
      ]
    },
    {
      "name": "Avendus",
      "aliases": [
        "Avendus Capital"
      ]
    },
    {
      "name": "YASH Technologies",
      "aliases": [
        "YASH Technologies Pvt Ltd"
      ]
    },
    {
      "name": "Qualitrol",
      "aliases": [
        "Qualitrol Company"
      ]
    },
    {
      "name": "GoKwik",
      "aliases": [
        "Go Kwik"
      ]
    },
    {
      "name": "CashFlo"
    },
    {
      "name": "Sanas",
      "aliases": [
        "Sanas AI"
      ]
    },
    {
      "name": "GreedyGame",
      "aliases": [
        "Greedy Game"
      ]
    },
    {
      "name": "InfiVR",
      "aliases": [
        "Infi VR",
        "Infinite Reality Labs",
        "Infi AR/VR"
      ]
    },
    {
      "name": "International Council on Clean Transportation",
      "aliases": [
        "ICCT"
      ]
    },
    {
      "name": "D Square Consulting Services Pvt Ltd",
      "aliases": [
        "D Square Consulting Services",
        "DSquare",
        "D Square Analytix"
      ]
    },
    {
      "name": "Subspace",
      "aliases": [
        "Grow90",
        "Subspace Money",
        "Whatsub"
      ]
    },
    {
      "name": "Lexsi Labs",
      "aliases": [
        "Lexsi.ai",
        "AryaXAI",
        "AryaXAI Alignment Labs",
        "Arya.ai"
      ]
    },
    {
      "name": "TurboHire",
      "aliases": [
        "TurboHire Technologies",
        "Turbo Hire Technologies Pte Ltd"
      ]
    },
    {
      "name": "Nuvama Wealth",
      "aliases": [
        "Nuvama Wealth Management",
        "Nuvama Private",
        "Nuvama Wealth and Investment",
        "Edelweiss Wealth Management"
      ]
    },
    {
      "name": "ALLEN Digital",
      "aliases": [
        "ALLEN Career Institute",
        "ALLEN",
        "Allen Digital Learning"
      ]
    },
    {
      "name": "SolarSquare",
      "aliases": [
        "SolarSquare Energy",
        "SolarSquare Energy Private Limited"
      ]
    },
    {
      "name": "Bristlecone",
      "aliases": [
        "Bristlecone India Limited",
        "Mahindra Bristlecone",
        "BristleconeLabs.ai"
      ]
    },
    {
      "name": "DigiPlus IT",
      "aliases": [
        "DigiPlus",
        "digiplus.co.in"
      ]
    },
    {
      "name": "Snackible",
      "aliases": [
        "LightSaber Food Ventures",
        "LightSaber Food Ventures Pvt Ltd"
      ]
    },
    {
      "name": "tech.at.core",
      "aliases": [
        "techatcore",
        "tech at core",
        "techatcore.com"
      ]
    },
    {
      "name": "FynTune Solution Private Limited",
      "aliases": [
        "FynTune",
        "Fyntune Solutions",
        "fyntune.com"
      ]
    },
    {
      "name": "WATRANA TRACTION PRIVATE LIMITED",
      "aliases": [
        "Watrana",
        "Watrana Traction Company",
        "watrana.com"
      ]
    },
    {
      "name": "ARGMAC",
      "aliases": [
        "Argmac Luxury",
        "Grinder Inc (IndiaMART seller name)",
        "argmac.com"
      ]
    },
    {
      "name": "HBeonLabs Technologies Private Limited",
      "aliases": [
        "Hbeonlabs",
        "HBeon Labs",
        "Hbeonlabs Technologies Pvt Ltd"
      ]
    },
    {
      "name": "METNMAT Research & Innovations",
      "aliases": [
        "Metnmat",
        "Metnmat Innovations Private Limited",
        "Metnmat Research and Development"
      ]
    },
    {
      "name": "LUXASIA",
      "aliases": [
        "LUXASIA Group",
        "L Beauty",
        "Luxasia Cosmetics India Private Limited"
      ]
    },
    {
      "name": "Intouch Quality Services Private Limited",
      "aliases": [
        "Intouch Group",
        "Intouch Quality Services Pvt Ltd",
        "intouchgroup.in"
      ]
    },
    {
      "name": "CAST",
      "aliases": [
        "CAST Software",
        "CAST Software India Private Limited",
        "CAST Highlight"
      ]
    },
    {
      "name": "Adfluence Hub",
      "aliases": [
        "AdfluenceHub",
        "adfluencehub"
      ]
    },
    {
      "name": "JSW Group",
      "aliases": [
        "JSW Steel",
        "JSW Energy"
      ]
    },
    {
      "name": "Dun & Bradstreet",
      "aliases": [
        "Dun and Bradstreet"
      ]
    },
    {
      "name": "Celanese"
    },
    {
      "name": "Copeland"
    },
    {
      "name": "IQVIA"
    },
    {
      "name": "Dana Incorporated",
      "aliases": [
        "Dana India"
      ]
    },
    {
      "name": "AtkinsRéalis",
      "aliases": [
        "AtkinsRealis",
        "Atkins Realis",
        "SNC-Lavalin"
      ]
    },
    {
      "name": "General Mills"
    },
    {
      "name": "Mondelēz International",
      "aliases": [
        "Mondelez International",
        "Mondelez"
      ]
    },
    {
      "name": "GlobalLogic"
    },
    {
      "name": "Visteon",
      "aliases": [
        "Visteon Corporation"
      ]
    },
    {
      "name": "Legrand"
    },
    {
      "name": "Flex",
      "aliases": [
        "Flextronics"
      ]
    },
    {
      "name": "Festo",
      "aliases": [
        "Festo India"
      ]
    },
    {
      "name": "FORVIA HELLA",
      "aliases": [
        "Forvia",
        "HELLA"
      ]
    },
    {
      "name": "Glencore"
    },
    {
      "name": "Wolters Kluwer"
    },
    {
      "name": "ICON plc",
      "aliases": [
        "ICON Clinical Research"
      ]
    },
    {
      "name": "Conduent"
    },
    {
      "name": "PTC",
      "aliases": [
        "PTC Inc",
        "Parametric Technology"
      ]
    },
    {
      "name": "West Pharmaceutical Services",
      "aliases": [
        "West Pharma"
      ]
    },
    {
      "name": "Avient",
      "aliases": [
        "Avient Corporation"
      ]
    },
    {
      "name": "NKT"
    },
    {
      "name": "PolyPeptide",
      "aliases": [
        "PolyPeptide Group"
      ]
    },
    {
      "name": "IFF",
      "aliases": [
        "International Flavors & Fragrances",
        "International Flavors and Fragrances"
      ]
    },
    {
      "name": "Crane NXT",
      "aliases": [
        "Crane Authentication",
        "Crane Currency"
      ]
    },
    {
      "name": "Calix"
    },
    {
      "name": "Veradigm"
    },
    {
      "name": "Everllence",
      "aliases": [
        "MAN Energy Solutions"
      ]
    },
    {
      "name": "Hill International"
    },
    {
      "name": "Hines"
    },
    {
      "name": "Russell Investments"
    },
    {
      "name": "Baker Tilly"
    },
    {
      "name": "CohnReznick"
    },
    {
      "name": "McDonald's",
      "aliases": [
        "McDonalds",
        "McDonald's Global Office in India"
      ]
    },
    {
      "name": "dentsu"
    },
    {
      "name": "Havas",
      "aliases": [
        "Havas Media Network India",
        "Havas Creative Network India"
      ]
    },
    {
      "name": "Teleperformance",
      "aliases": [
        "TP"
      ]
    },
    {
      "name": "Numerator"
    },
    {
      "name": "UST",
      "aliases": [
        "UST Global"
      ]
    },
    {
      "name": "AML RightSource"
    },
    {
      "name": "Etraveli Group"
    },
    {
      "name": "Air Arabia"
    },
    {
      "name": "Freight Tiger"
    },
    {
      "name": "FanCode"
    },
    {
      "name": "Enterpret"
    },
    {
      "name": "BiteSpeed"
    },
    {
      "name": "Hevo Data",
      "aliases": [
        "Hevo"
      ]
    },
    {
      "name": "HighLevel"
    },
    {
      "name": "Unicommerce"
    },
    {
      "name": "Kapture CX",
      "aliases": [
        "Kapture"
      ]
    },
    {
      "name": "SiteRecon"
    },
    {
      "name": "Snapmint"
    },
    {
      "name": "Wint Wealth"
    },
    {
      "name": "KYC Hub"
    },
    {
      "name": "Dodo Payments"
    },
    {
      "name": "API Holdings",
      "aliases": [
        "PharmEasy"
      ]
    },
    {
      "name": "Honasa Consumer",
      "aliases": [
        "Honasa",
        "Mamaearth"
      ]
    },
    {
      "name": "Kapiva"
    },
    {
      "name": "Blissclub"
    },
    {
      "name": "Plix"
    },
    {
      "name": "Mosaic Wellness"
    },
    {
      "name": "Zolo"
    },
    {
      "name": "WeWork India",
      "aliases": [
        "WeWork"
      ]
    },
    {
      "name": "Pickyourtrail"
    },
    {
      "name": "WedMeGood"
    },
    {
      "name": "Rusk Media"
    },
    {
      "name": "Vecmocon",
      "aliases": [
        "Vecmocon Technologies"
      ]
    },
    {
      "name": "HaystackAnalytics"
    },
    {
      "name": "Medanta"
    },
    {
      "name": "Trilegal"
    },
    {
      "name": "Ujjivan Small Finance Bank",
      "aliases": [
        "Ujjivan"
      ]
    },
    {
      "name": "Resollect"
    },
    {
      "name": "Teli Labs"
    },
    {
      "name": "Fynd"
    },
    {
      "name": "AppsForBharat"
    },
    {
      "name": "CredFlow"
    },
    {
      "name": "Datacultr"
    },
    {
      "name": "Volody"
    },
    {
      "name": "LineupX"
    },
    {
      "name": "V2 Retail",
      "aliases": [
        "V2 Retail Ltd",
        "Vishal Retail"
      ]
    }
  ],
  "_acronym_alias_note": "A watchlist term under 4 characters can only ever match EXACTLY, because matchCompany refuses to substring-match short terms (that guard is what stops 'Navi' matching every company with Navi Mumbai in its name). So an acronym-only entry never fires against the full name LinkedIn actually shows. 'HPE' was on the list the whole time while 'Hewlett Packard Enterprise' was dropped 10 times as off-watchlist. Same for GE Vernova, PwC Acceleration Center India and JSW Steel. If you add an acronym, add the spelled-out name as an alias too.",
  "_added_from_linkedin_note": "Employers first noticed in LinkedIn's own recommendations, each posting a real engineering internship the watchlist was turning away. Added by hand on purpose. A broad sweep is not a shortcut here: the off-watchlist employers posting the MOST engineering internships are almost entirely training schemes and staffing spam - MediNex Workforce alone posted 34, and 'Future Interns', 'kaushalsaathi' and 'EazyByts' follow it. Genuine companies post once or twice, so frequency ranks the junk to the top. Remove one with `npm run remove -- \"<name>\"`.",
  "searchMode": "keywords",
  "_searchMode_note": "'keywords' (default) = run the searches list below; with a single broad entry that means one sweep paginated to exhaustion, filtered locally. 'companies' = boolean OR batches of the watchlist - NOT recommended: LinkedIn treats those names as loose text and returns mostly unrelated employers. 'both' = company batches then the searches list.",
  "companySearch": {
    "batchSize": 30,
    "_batchSize_note": "Companies per query. Batches are also capped by URL length, so a group of long names may hold fewer than this.",
    "extraKeywords": "",
    "_extraKeywords_note": "Prepended to every batch query. Deliberately empty: filters.jobTypes already restricts results to internships, and adding the literal word 'internship' would ALSO require it to appear in the job text - excluding postings titled e.g. 'SDE Intern' that never spell it out. Set it only if you want that narrowing."
  },
  "defaultLocation": "India",
  "_defaultLocation_note": "Applied to any search written as a plain string. Change this one value to move the whole watch to another city or country.",
  "_searches_note": "One broad search PER REGION, paginated to exhaustion, then filtered locally by title, role and company. The boolean form matters: a plain 'internship' keyword misses postings titled only 'Intern' (Salesforce's 'Summer 2027 Intern - Software Engineer' is one), and the OR list covers every phrasing the title filter accepts. Keep it to one entry per region - each extra search costs a full pagination pass against the single account everything depends on, so add a region only when its board is worth the requests, and see limits.searchesPerRun for how to make regions take turns instead.",
  "_searches_region_note": "'region' is what a card's location falls back to when LinkedIn renders none, and it is also the key each region's own lookback window and covered-ground baseline are stored under. 'geoId' is what actually scopes the results: the redirect to /jobs/search-results/ DROPS 'location=' but keeps 'geoId', so the location string is cosmetic and the id is load-bearing. India's entry deliberately carries NO geoId - it has always come back Indian from account geo bias alone, and sending an id would change what LinkedIn returns on the collector that supplies 91% of the India board. The ids live in src/regions.js.",
  "searches": [
    {
      "keywords": "(intern OR internship OR trainee OR \"co-op\" OR apprentice)",
      "region": "IN"
    },
    {
      "keywords": "(intern OR internship OR trainee OR \"co-op\" OR apprentice)",
      "region": "US",
      "location": "United States",
      "geoId": 103644278,
      "intervalMinutes": 120,
      "minWindowHours": 2,
      "windowMarginHours": 0.75
    }
  ],
  "_searches_window_note": "The US search narrows its own lookback. The global filters are minWindowHours 3 and a 2-hour slack on top of the gap, both tuned when this was one India search on a 30-minute loop - and on a dense region they are wildly too wide. Measured: US results run about 161 cards an hour, so a 3-hour window is roughly 21 pages, and the US sweep was walking every one of them to exhaustion to collect a single hour of new postings, because two thirds of what LinkedIn returned was newer than the covered-ground horizon and kept resetting the early stop. An hourly sweep needs the gap (1h) plus the 45-minute margin the horizon already uses, so 2 hours - about 14 pages. This changes the FLOOR and the SLACK, not the rule: a region missed for six hours still stretches its window to cover the gap.",
  "_searches_interval_note": "'intervalMinutes' makes ONE search run less often than the loop ticks, which is different from limits.searchesPerRun - that dial makes every region take turns at the same rate, this one sets a rate per region. India has none, so it is swept every 30-minute tick and keeps the freshness the board promises. The US runs hourly: its supply is dense and steady all day (24 intern-titled cards in a 12-minute window when measured), so half the sweeps buys most of the roles at half the page loads against the one account everything depends on. Skipping is not failing - the region's baseline is left alone, so the next sweep's adaptive window has stretched to cover the gap and nothing is missed.",
  "filters": {
    "adaptiveWindow": true,
    "_adaptiveWindow_note": "Sizes the lookback from how long ago the last successful run was, instead of using one fixed number. With hourly runs a fixed 30-hour window would re-paginate 30 hours of postings every time to find the newest hour - roughly 30x the page loads for nothing. After a normal hourly run the window is ~3h; after an overnight sleep it stretches to cover the gap, so nothing is missed.",
    "minWindowHours": 3,
    "maxWindowHours": 36,
    "postedWithinHours": 30,
    "_postedWithinHours_note": "Used only when adaptiveWindow is false, or when there is no previous run to measure from.",
    "sortBy": "recent",
    "jobTypes": [],
    "_jobTypes_note": "DELIBERATELY EMPTY. Setting this to ['INTERNSHIP'] sends f_JT=I, which trusts the recruiter's employment-type tag - and they get it wrong constantly. A real case: Salesforce's 'Summer 2027 Intern - Software Engineer' is tagged Full-time, so f_JT=I hid it completely. The title filter and role classifier below decide what is an internship instead, from the title itself. LinkedIn f_JT codes if you ever want them: F=full-time P=part-time C=contract T=temporary I=internship V=volunteer O=other."
  },
  "roleClassifier": {
    "_useGemini_note": "Currently OFF - the offline vocabulary in src/roles.js decides. Gemini was never the speed bottleneck (it runs as one batch after the walk, not per job), but the offline classifier is instant and free, so there is little reason to switch it on unless the vocabulary starts missing things. When true: ask Gemini whether each captured role is a software role. Needs GEMINI_API_KEY in the environment - put it in a .env file at the project root (gitignored) and it is picked up automatically. Titles are sent as ONE batched request per run, not one per job. If the key is missing, the quota is spent, or the call fails for any reason, the offline vocabulary in src/roles.js decides instead - a job is never left unclassified because an API was down.",
    "_useGeminiForAmbiguous_note": "The one place Gemini still earns its keep. A title the vocabulary cannot settle - a bare 'Trainee', 'Apprentice', or something resting only on the word 'Engineer' - gets its DESCRIPTION read instead. Gemini also names the phrase its decision turned on, and that phrase is added to the offline vocabulary, so the same kind of title is answered instantly and free from then on. The vocabulary therefore gets better and the API gets called less the longer this runs. Vetted before anything is learned: the phrase must actually appear in the posting, must not contradict a built-in rule, and must be a sane length.",
    "model": "qwen3:8b",
    "useModel": true,
    "useModelForAmbiguous": true,
    "_model_note": "Ollama model tag. Same model as the enricher so only one stays resident in memory."
  },
  "matching": {
    "storeNonTechRoles": false,
    "_storeNonTechRoles_note": "false = a confidently non-engineering title is discarded during the sweep instead of being stored. The site is engineering-only, so storing them meant carrying ~80% of rows that could never be published, and spending description backfills on them. They are still recorded in seen_cards so the next run does not re-decide the same card. Set true to collect them again (and set publish.techRolesOnly false to show them).",
    "openNonTechRoles": false,
    "_openNonTechRoles_note": "false = a role the classifier CONFIDENTLY calls non-tech is listed from its search-card data alone, with no page open. Ambiguous titles are still opened, since that is exactly when the description decides. On a real backfill this cut opens from 12 to 4 per dozen candidates. Card-only listings have no description, summary or skills, which is fine for the Other roles tab. Set true to open everything.",
    "skipViewedCards": false,
    "_skipViewedCards_note": "OFF. LinkedIn marks a card Viewed whether the scraper opened it or you did, so browsing LinkedIn yourself made those postings permanently invisible to your own site - 93 watchlist listings were lost that way, including GE HealthCare, Citi, Honeywell, Times Internet and Infineon. The re-opening it was meant to prevent is already prevented by seen_cards and the run lock, so the protection was redundant and the cost was real. Set true only if runs start re-opening cards they have already processed.",
    "requireCompanyMatch": true,
    "_requireCompanyMatch_note": "true = the FIRST gate. A posting from a company not on the watchlist is skipped outright - no title parsing, no role classification, no Gemini call. Set false to consider every internship found, which is far more work and spends classifier quota on employers you do not care about.",
    "titleMustMatch": [
      "intern",
      "internship",
      "trainee",
      "co-op",
      "coop",
      "summer analyst",
      "apprentice"
    ],
    "_titleMustMatch_note": "A job must contain one of these in its title. Use [] to disable.",
    "_roles_note": "Engineering roles only. A confidently non-technical title is discarded during the sweep (storeNonTechRoles) and never published (publish.techRolesOnly). An AMBIGUOUS title is still opened and judged on its description - that is exactly the case the title cannot settle, and dropping those on the title alone would lose real engineering roles posted as a bare 'Trainee'.",
    "extraTechTerms": [],
    "extraNonTechTerms": [
      "financial",
      "indirect tax",
      "technical support",
      "support representative",
      "credit risk",
      "fraud risk",
      "market risk",
      "credit analyst",
      "treasury",
      "financial crime",
      "anti-money laundering",
      "regulatory compliance",
      "compliance analyst",
      "underwriting",
      "actuarial",
      "business intern",
      "business analytics",
      "product manager",
      "product management",
      "associate product manager",
      "product design",
      "product designer",
      "product analyst",
      "product owner",
      "product marketing",
      "technical writer",
      "technical writing",
      "technical documentation",
      "content writing",
      "civil engineering",
      "mechanical engineering",
      "chemical engineering",
      "electrical engineering",
      "industrial engineering",
      "manufacturing engineering",
      "process engineering",
      "structural engineering",
      "structural",
      "electrical",
      "highway",
      "foundry",
      "aerospace engineering",
      "materials engineering",
      "petroleum engineering",
      "environmental engineering"
    ],
    "titleOnlyNonTechTerms": ["technical support", "electrical"],
  "_titleOnlyNonTechTerms_note": "Terms that veto on the employer's TITLE but never on the model's roleLabel. A label is the model summarising a description in its own words, so a blunt term misfires there. 'technical support' dropped HPE College Intern roles that require a CS degree and Python/Java/C++, because the duties say troubleshoot and support. IBM's Technical Support Representative Intern still says it in the title and is still refused. Measured over 30 days: 10 vetoed rows, 7 by title (right), 2 by label (wrong), 1 ambiguous.",
  "_slashTerms_note": "The three that whole-word matching could not see, added 1 Sep 2026. 'Civil/Highway Engineer' and 'Diploma Trainee - Electrical / Civil' put a slash where the phrase needed a space, so 'civil engineer' and 'electrical engineering' both missed them. Fixed with the words the titles DO contain rather than by teaching the matcher about slashes: 'highway' (2 stored titles, both roads/civil, no software use), 'foundry' (1 title, metal casting - and note the word boundary means it can never match 'GlobalFoundries', which is why the semiconductor worry was overstated) and 'electrical'. ELECTRICAL IS TITLE-ONLY. As a full negative it refused 18 rows with no software signal, but two of them were label-driven and both ambiguous: Rocket Lab's 'Avionics Intern - Neutron Avionics Systems' (label 'Electrical System Design') and Valeo's 'R&D Trainee' (label 'Electrical Systems Development'), which are as likely to be embedded firmware as hardware. Same shape as 'technical support' dropping HPE's CS-degree internships. Scoped to the title it takes the 16 real ones - SpaceX Electrical x3, Astranis, Zipline, Emerson, Raytheon, Norfolk Southern, Parsons, Axcelis, Qorvo, GlobalFoundries facilities - and spares both. 'computer engineering' was added to POSITIVE in roles.js in the same change, so a future 'Electrical and Computer Engineering Intern' is rescued by its own discipline rather than refused by the other one.",
  "_civilNotAdded_note": "A bare 'civil' was measured and REFUSED as a term. Of 22 stored titles containing it, 21 are civil engineering (SpaceX, AECOM, Kiewit, Jacobs, Hill, AtkinsRealis) and exactly one is a real software role - Palantir's 'Privacy & Civil Liberties Engineer - New Grad'. Only two of the civil ones are live, so the term would fix two rows and cost a genuine Palantir engineering listing. Same call as 'apm'. The slash forms ('Civil/Highway Engineer', 'Electrical / Civil') are left leaking for the same reason: whole-word matching cannot see them and the only fix is a term too broad to be safe. 'foundry' was also refused - one row, and it would put semiconductor foundry roles at risk. Bare 'structural' IS safe: all 14 stored titles containing it are structural engineering and none is software.",
  "_disciplineTerms_note": "The -ing forms of the non-software engineering disciplines, added 1 Sep 2026. roles.js already carried 'civil engineer', 'mechanical engineer', 'chemical engineer', 'electrical engineer', 'production engineer' and 'industrial engineer' as negatives, plus bare 'mechanical' and 'chemical' - but matching is whole-word, so 'civil engineer' never matched 'Civil Engineering' and 'industrial engineer' never matched 'Industrial Engineering'. Only the two bare words were doing any work. These are safe against real software roles for the same reason every negative here is: a SPECIFIC multi-word positive still outranks them, so 'Software Engineering Intern - Manufacturing Systems' survives via 'software engineering'. They are paired with GENERIC_POSITIVE in roles.js - without that change 'engineering intern' cancelled every one of them.",
  "_writingTerms_note": "Technical writing is not engineering. 'technical' is a POSITIVE single word, so 'Technical Writer Internship' classified tech through it and PearlThoughts' writing internship went live on the India board on 1 Sep 2026. A negative beats a single-word positive (only a MULTI-word positive outranks one), so the phrase settles it. Measured over 3,955 stored rows these four terms refuse exactly ONE row - that PearlThoughts posting - and nothing else, so there is no collateral. 'documentation' alone was deliberately NOT added: it would take Tonbo Imaging's 'Intern Junior Engineer - Manufacturing Engineering Documentation' and anything like 'API Documentation Engineer'. 'content writer' and 'copywriting' were already here and already worked.",
  "_productTerms_note": "Product management joined this list on 1 Sep 2026, and was REMOVED from the positive list in src/roles.js at the same time — listing 'product manager' as a POSITIVE is why Salesforce's 'Summer 2026 Intern - Product Manager' reached the top of the India board. A PM or product-design internship is not an engineering internship. THE BARE WORD 'product' WAS MEASURED AND REJECTED: over 3,955 stored rows the four phrases refuse 85 rows and cost nothing, while a bare 'product' refuses 114 and the extra 29 include Micron's 'Intern - ML/AI Engineer (Product Engineering, STPG)', three more Micron semiconductor roles, Qorvo's 'Product Engineer D&A Intern', John Deere and Emerson Product Engineering, Seclore's 'Trainee- Product Engineer' and Tesla's 'Product Support Engineer' - plus label collateral like Silicon Labs' validation internship and Adobe's 'Apprentice Tech'. Same shape as the bare 'business' and 'risk analyst' terms this file already records as tried and dropped. 'Product Engineering Intern' survives the precise list because 'engineering intern' is a multi-word positive and a negative only wins when no multi-word positive matches. 'apm' was left OUT of this list on purpose: it also means Application Performance Monitoring, and as a negative it refused Philips' 'Co-op - Software Engineering (APM)'.",
  "_extraNonTechTerms_note": "Matching is whole-word, so the built-in 'finance' never matched 'Financial Analyst' and 'taxation' never matched 'Indirect Tax'. Those fell through as uncertain, and since an uncertain title is now published rather than held, six Amazon finance roles would have gone onto an engineering-only site. Remove 'financial' if a title like 'Financial Systems Software Engineer' ever gets wrongly dropped.",
    "_riskTerms_note": "The risk/compliance block was added 16 Aug 2026 after 'Apprentice - Credit Risk Analyst' at American Express went live on an engineering board. Amex posts EVERY internship as the bare word 'Apprentice', so the title can never be settled and the description decides; the model called it tech because the prompt counts 'data science and analytics' as tech and the role does run on SQL, Python and Tableau. These terms are matched against the model's own role label as well as the title (see vetoNonTech). They are safe against genuine engineering roles inside risk teams: a negative only wins when no MULTI-WORD positive also matches, so 'Credit Risk Data Engineer' still classifies tech via 'data engineer'. Every term names a FINANCE domain on purpose. A bare 'risk analyst' / 'risk management' was tried first and dropped: it flipped Zscaler's 'Insider Risk Analyst', which is security operations and squarely in scope. The domain-specific terms already caught every bad row, so the broad ones bought nothing and cost a real listing.",
    "_extraTerms_note": "Your own additions to the classifier, checked ahead of the built-in lists.",
    "blocklist": [
      "MedTourEasy",
      "Silicon Global Consulting"
    ],
    "_blocklist_note": "Employers never published, whatever the watchlist says. Checked before any watchlist term, so a fuzzy match cannot resurrect one. Matching is containment on the normalised name, so \"MedTourEasy\" also covers \"MedTourEasy Navi Mumbai\". MedTourEasy is here because it was reported as a scam; it only ever reached the site because \"MedTourEasy Navi Mumbai\" contains the city Navi Mumbai, which the matcher read as the fintech Navi."
  },
  "pacing": {
    "_note": "All values in milliseconds, randomised per action. These were roughly halved for speed, which roughly doubles the request rate - a real increase in ban risk. If LinkedIn ever rate-limits, raise these first; the 24h cooldown will have already stopped the run.",
    "betweenCards": [
      1800,
      3600
    ],
    "betweenPages": [
      4000,
      8000
    ],
    "betweenSearches": [
      20000,
      40000
    ],
    "scrollStep": [
      260,
      700
    ],
    "afterNavigation": [
      1500,
      3000
    ],
    "warmupOnFeed": [
      3000,
      6000
    ],
    "longBreakEvery": 30,
    "longBreak": [
      12000,
      25000
    ],
    "_longBreak_note": "Every N job cards, take a longer human-style break.",
    "startupJitter": [
      0,
      60000
    ],
    "_startupJitter_note": "Scheduled runs wait a random extra 0-60s, so activity does not land at exactly HH:00:00 every time. Was 0-4 min when runs were half-hourly; at 15-minute slots that much jitter could push a run most of the way into the next slot, where the lock would then skip it."
  },
  "limits": {
    "maxRuntimeMinutes": 90,
    "maxPagesPerSearch": 40,
    "maxDetailsPerRun": 100,
    "searchesPerRun": 0,
    "_searchesPerRun_note": "How many of the searches above one run may walk. 0 (the default) means all of them, so every region is swept every tick. Set it to 1 to make the regions TAKE TURNS: the rotation cursor already remembers where the last run stopped, so nothing is skipped - each region is simply swept half as often and total request volume stays exactly where it was on a single region. That matters because LinkedIn dropped this session twice in August without ever answering 429, and page loads are the budget a rate limit is eventually spent on. Raise the region count and this is the first dial to reach for.",
    "_maxRuntimeMinutes_note": "There is no schedule slot to overrun any more, so this no longer has to stay under the interval. bin/loop.sh runs one scan at a time and starts the next when the previous one finishes, so a long run costs nothing but its own time - it can never collide with, or be skipped by, another run. Raised from 20 to 90 so a scan is not cut off mid-pagination: the budget is a backstop against a genuinely stuck run, not the normal stopping point. Paging normally ends when LinkedIn's Next button runs out.",
    "_note": "Safety backstops, not the normal stopping point. Paging normally ends when LinkedIn's 'Next' button runs out, which means every job in the window has been seen. These caps only bite if a search is unusually deep - and when one does bite, the report says so explicitly rather than pretending the search finished.",
    "_maxPagesPerSearch_note": "40 pages x 25 = 1000 results, which is roughly LinkedIn's own ceiling for a single search. With the 24-hour filter on, most searches end far sooner."
  },
  "_whatsapp_note": "Broadcasting India's new listings to WhatsApp by driving WhatsApp Web through Playwright. THERE IS NO PUBLIC CHANNEL API, so this automates the web client, which is against WhatsApp's Acceptable Use policy and whose documented consequence is the NUMBER being banned - it runs on a throwaway number, on its own Brave profile, slowly, and defaults to OFF. `enabled` stays false until `npm run whatsapp-login` has linked the number and `npm run whatsapp-probe` confirms the target is reachable. `target` is the channel or group by the name it shows in WhatsApp. `sendGapMs` is deliberately slow: this is a UI being driven, not an API, and a burst is what gets a number flagged. `maxPerRun` bounds one scan - the rest wait for the next, which is the same call the reel sweep makes.",
  "github": {
    "enabled": true,
    "repo": "akshat0011/engineering-internship-india",
    "region": "IN",
    "_note": "The public GitHub internship list. DAILY, not per publish: the repo history is public and regenerating on every scan would be 48 commits a day of noise, even though the README is deterministic and would usually be unchanged. Daily is also a promise that can always be met - the README says 'Updated Daily' - whereas hourly cannot be, because this Mac sleeps. Asked on every scan and answered once, the same shape as the weekly roundup and the discovery sweep, and for the same reason: a cron entry firing at a fixed time is simply missed when the machine is asleep, while this lands on the first scan after it wakes. India only, because that is the gap - SimplifyJobs already owns the US equivalent with tens of thousands of stars and there is no Indian one. enabled:false stops it without unpicking anything.",
    "_checkout_note": "The repo is cloned to PATHS.ghList (~/Library/Application Support/interndoor/gh-list). It is deliberately NOT inside app/, which is itself a public repo the scheduler pushes to every 30 minutes - a nested checkout is how one repo ends up committed into the other. A missing checkout is reported once a day and exits 0; this must never be able to fail a scan."
  },
  "whatsapp": {
    "enabled": true,
    "regions": ["IN"],
    "target": "Interndoor",
    "sendGapMs": 6000,
    "maxPerRun": 8,
    "headless": true
  },
  "notifications": {
    "onCaptcha": true,
    "onNewJobs": true,
    "onError": true,
    "openReportWhenDone": true,
    "_openReportWhenDone_note": "Auto-open the HTML report in your default browser when new jobs are found IN THE HOME REGION - see homeRegion below.",
    "_reportRegions_note": "Which boards the LOCAL RUN REPORT shows, behind a toggle. Separate from homeRegion on purpose: homeRegion decides what INTERRUPTS him (banner, phone push, auto-opening the report) and is about roles he will apply to himself, while the report is also where LinkedIn posts are written for the InternDoor page, which targets the US. A US role belongs in the report and does not belong on his phone at 2am. The boards are kept apart by a toggle rather than merged, because every counter on that page - companies, stipend listed, easy apply - means nothing across two countries at once.",
    "reportRegions": ["IN", "US"],
    "homeRegion": "IN",
    "_homeRegion_note": "The only region whose new listings interrupt HIM: the Mac banner, the phone push, and auto-opening the report. Everything that serves READERS is unaffected - every region is still collected, still published, and still posted to its own Telegram channel. He applies to internships in India, so a US listing is worth all of those things and worth nothing as a banner on his Mac at 2am. Left unscoped this got much worse the moment US collection went live: one run produced 86 new listings, 76 of them American, so an alert meant to say 'apply in the first hour' would mostly announce roles he will never open. Set to 'all' to be alerted about every region, or to another ISO code if he ever moves. The report itself still contains every region and is still written every run - only the decision to OPEN it is scoped, because its post-queue buttons are useful for a role he would post about but not apply to.",
    "_whatsapp_channels_note": "The WhatsApp channel's invite link, read by src/channels.js for the /alerts page and the Get alerts pill. Same rule as Telegram: a region with no entry gets NO link rather than another region's. WhatsApp is listed AHEAD of Telegram where a region has both - an ordering claim about which app the reader already has open, not a ranking, and Telegram is still offered.",
    "whatsapp": { "channels": { "IN": "https://whatsapp.com/channel/0029VbEJ2qLBA1eq3S5hBE3P" } },
    "telegram": {
      "enabled": true,
      "chatId": "@interndoor",
      "channels": {
        "IN": "@interndoor",
        "US": "@interndoorusa"
      },
      "_channels_ids_note": "Numeric ids, for recovery if a channel is ever renamed: IN @interndoor = -1004300938042, US @interndoorusa = -1004479304597. The usernames are used above because they are readable, but a rename breaks them and the numeric id cannot change - this project renamed its brand twice in one day, so keep these written down. Verified 25 Aug: @Internzobot is an administrator of both with can_post_messages true. GB has no channel and therefore gets no post.",
      "_channels_note": "One channel per region, keyed by ISO code. A region with no entry here gets NO post - posting US listings to people who subscribed for internships in India is how a channel gets muted. chatId above predates regions and is kept as India's fallback, so an empty channels map behaves exactly as before. Only published regions are posted at all: a region that is collected but not published has no pages written for it, so every link would 404.",
      "_note": "Posts one message per run listing the new roles, each linking to its page on the site. One message rather than one per job: a run that finds six roles firing six notifications is how a channel gets muted. Set enabled to true and chatId to your channel (\"@yourchannel\", or the numeric -100... id for a private one), and put TELEGRAM_BOT_TOKEN in .env. The bot must be an administrator of the channel with permission to post, or Telegram returns 403 and the post is skipped. Everything here fails soft - a channel that is misconfigured, rate-limited or down never fails the scrape."
    }
  },
  "browser": {
    "headed": true,
    "_headed_note": "Must stay true. Headless Chromium reports 'HeadlessChrome' in its user agent and an 800x600 screen - trivially detectable, and you could not solve a CAPTCHA in a window you cannot see. The tool refuses to start if this is false.",
    "windowSize": [
      1512,
      950
    ],
    "windowPosition": [
      40,
      40
    ],
    "_windowPosition_note": "Where the Brave window opens. Set to [-2400, -2400] to park it off-screen if you find it distracting - but then you would have to drag it back into view to solve a CAPTCHA.",
    "returnFocus": true,
    "_returnFocus_note": "Brave always steals keyboard focus when it launches. This hands focus straight back to whatever app you were using, so a run at 12:00 does not interrupt you.",
    "disableBraveShields": true,
    "_disableBraveShields_note": "Passes --disable-brave-extension. Mostly belt-and-braces: on a fresh profile Shields has no filter lists to work from, and LinkedIn was verified to load fine either way."
  },
  "regions": {
    "_note": "Where postings are collected from, and where they are shown. Collecting a region and publishing one are separate settings on purpose: collection is cheap and reversible - an ATS board is read once whatever its rows turn out to be - while publishing a region is a public commitment to a board that has to look worth visiting. Keeping them apart is what lets a region fill up quietly for a month before anyone is shown it.",
    "collect": "all",
    "_collect_note": "'all' (default) collects every region src/regions.js knows, plus 'unknown'. A location the gazetteer cannot read is STORED and never published, so a better gazetteer reads it later without re-collecting anything. An array of ISO codes narrows it, e.g. [\"IN\",\"US\"].",
    "publish": [
      "IN",
      "US",
      "GB"
    ],
    "_publish_note": "ISO codes, in the order the region switch lists them. India is served at the site ROOT and every other region at /<slug>/ - the slugs are in src/regions.js, and GB is served at /uk/. Add a code here only once that region's board holds enough live roles to be worth a visit: node -e \"import('./src/store.js').then(m=>console.log(new m.Store().regionCounts({sinceMs:Date.now()-14*86400000})))\"",
    "_urls_note": "India stays at the ROOT permanently. Its ~130 job pages and 125 company hubs are already indexed at /jobs/... and /companies/...; moving them under /in/ to make the scheme tidy would 404 every one of them at once. This repo has already paid that bill - 198 company hubs were deleted and rebuilt by an earlier bug, and the churn suppressed them for weeks."
  },
  "discovery": {
    "_note": "Find postings an employer does not index on its own board, by asking Google Programmable Search. Amazon's India board lists 2,538 roles and four internships, none engineering, while its SDE internship sits at a live URL Amazon's own search will not return - and Google has that page, because it is public, server-rendered and allowed by robots.txt. Runs ONCE A DAY from bin/run.sh: the free tier is 100 queries a day and the scheduler fires 48 times, so a per-scan sweep would spend the quota by lunch for no extra freshness. Needs GOOGLE_CSE_KEY and GOOGLE_CSE_CX in .env; without them it is a silent no-op.",
    "enabled": true,
    "sites": [
      "amazon.jobs"
    ],
    "_sites_note": "One search per site per query, so sites x queries IS the daily budget. Keep it to boards whose own search is known to be poor - anything with a working board is already read by bin/poll-ats.js far more cheaply.",
    "queries": [
      "intern India",
      "internship 2027 India"
    ],
    "_queries_note": "Narrow on purpose. A broad query returns careers landing pages, 'life at' articles and PDFs, and each of those costs a fetch to discover it is not a posting.",
    "dateRestrict": "d7",
    "_dateRestrict_note": "Google's freshness window: d7 = the last week. An internship is worth finding in its first week; without a limit every sweep returns the same aged pages and spends its budget re-checking them.",
    "maxPerRun": 20,
    "_maxPerRun_note": "How many NEW links are resolved per sweep. Already-seen URLs are remembered in the discovered_urls table and never re-fetched, so this only bites on a first run or a sudden burst."
  },
  "publish": {
    "enabled": true,
    "autoPush": true,
    "_autoPush_note": "After each run, commit web/public/data/jobs.json and push. Vercel is watching the repo, so the push is what makes the public site update.",
    "maxAgeDays": 30,
    "_maxAgeDays_note": "How long a job stays on the public site after it was first seen. Raised 14 -> 30 on 24 Aug against measured data, not taste. ATS rows carry ground-truth liveness (they are dropped when they leave the board, see recentJobs) so this window only ever governed LINKEDIN rows, which have none - only 4% of them are ever re-seen a day later, because the search is time-windowed and a card falls out of it almost immediately. Measuring how long ATS internships actually stay open gives median 18 days and p75 30.5, so a 14-day cut was deleting pages for roles STILL ACCEPTING APPLICATIONS 56% of the time - and deleting them at roughly the age they would start to rank. 30 days covers three postings in four for their whole open life. It is a proxy and it will sometimes serve a closed role; the cost of that is mild, because a LinkedIn apply_url lands on LinkedIn's own 'no longer accepting applications' page, whereas the cost of deleting an open one is the whole page. Google requires an EXPIRED posting to stop being served; it sets no maximum age.",
    "_regions_publish_note": "Which regions get a published board is set in the top-level 'regions' block, not here. It replaced publish.indiaOnly, which was a boolean on a site that only ever had one board.",
    "techRolesOnly": true,
    "_techRolesOnly_note": "Only postings the classifier called technical reach the site. Applied at publish time, not in SQL, so rows stored back when the site carried every role simply stop appearing - nothing is deleted and flipping this to false brings them all back. A row whose verdict has not run yet (is_tech NULL) is NOT published: unclassified must not be treated as maybe-technical.",
    "includeFullDescription": false,
    "_includeFullDescription_note": "Leave false. Full descriptions are the posting company's copyrighted text; the site shows your generated summary and links to the original instead."
  },
  "indexing": {
    "enabled": true,
    "_note": "Google Indexing API. Announces each new JOB PAGE so it is crawled in hours instead of waiting weeks for a crawl it may never get - a job page lives 30 days here, so a page discovered slowly is a page whose whole life passed unseen. DOES NOTHING until a service-account key exists at ~/Library/Application Support/interndoor/google-indexing-key.json AND that account is an OWNER of the property in Search Console. src/indexing.js has the four setup steps.",
    "dailyCap": 190,
    "_dailyCap_note": "URLs per rolling 24h. Google's default allowance is 200 and this is CLAMPED to it in code - a bigger number is not a bigger allowance, it is the same allowance plus refusals. 190 leaves headroom for hand-run submissions and for the small undercount in indexCountSince. This board runs ~110 new job pages a day plus about as many expiries, so it sits just under the ceiling: request a quota increase from the API console before the US board grows.",
    "perRun": 25,
    "_perRun_note": "Bounds the wall-clock this adds to a scan, nothing else. The queue drains over days regardless, so spending a whole day's budget in one publish buys no freshness. 25 URLs is a few seconds.",
    "minAgeMinutes": 5,
    "_minAgeMinutes_note": "How long a URL waits after being queued before it may be sent. The API makes Google FETCH the page, and a push takes Vercel up to a minute to deploy - announcing sooner invites a crawl of the previous build, or a 404. In practice this means a page published now goes out on the next run.",
    "indexNow": {
      "enabled": true,
      "key": "96d97088a61babe560d257ceb8408820",
      "_note": "IndexNow — Bing, and through Bing also Yahoo and much of DuckDuckGo, plus Yandex, Seznam and Naver. Unlike Google's Indexing API it accepts ANY page type, which is the whole reason it is here: hubs, the directories, /alerts and /report cannot be announced to Google at all and would otherwise wait for an ordinary crawl. It submits only pages whose bytes CHANGED this publish, which is what Bing asks for - re-sending an unchanged page every 30 minutes is the abuse the protocol warns against.",
      "_key_note": "The SAME string must exist at web/public/<key>.txt AND be in the PUBLISHED allowlist in src/publish.js. If that file is not live, every submission is refused 403 and nothing says so on the site."
    },
    "maxAttempts": 3,
    "_maxAttempts_note": "After this many failures a URL is retired from the queue rather than retried forever. It keeps its error text, so `npm run index-urls -- --status` still shows why."
  },

  "safety": {
    "pauseOnChallenge": false,
    "_pauseOnChallenge_note": "TEMPORARILY OFF. true = a detected security check pauses the run, alerts you, and waits captchaWaitMinutes for a human. It was firing on pages with no CAPTCHA on them: the detector matches page TEXT as well as frames, and the challenge markers - unlike the logged-out ones - are not suppressed when the signed-in navigation is visible, so an ordinary posting containing a phrase like 'security verification' stopped a healthy run. Each false pause also held the Brave profile for 4 minutes, which is what made the FOLLOWING run die with 'Brave would not launch after 3 attempts'. With this off the run screenshots the page (screenshots/challenge-ignored-*.png) and carries on. Turn it back on once the detector is fixed - the screenshots are the evidence for that work.",
    "captchaWaitMinutes": 4,
    "_captchaWaitMinutes_note": "How long to wait for a human to solve a security check. Was hardcoded at 12 minutes with no knowledge of the run budget, which is how a run reached 59 minutes on a 15-minute schedule - it blew past maxRuntimeMinutes, its lock aged out and was treated as stale, and the next run started while this Brave still owned the profile and died on a launch timeout. Also capped at the run's remaining budget, so this is a ceiling and not a promise.",
    "cooldownHoursAfterRateLimit": 24,
    "_cooldownHoursAfterRateLimit_note": "If LinkedIn rate-limits or restricts the session, the tool refuses to run again for this many hours instead of walking back into it. This is the one setting here with real account risk - repeatedly walking back into a rate limit is how a session gets restricted. It was temporarily 0 while the run failures were being diagnosed; restored to 24 on 11 Aug 2026 after LinkedIn invalidated the session outright."
  },
  "summarizer": {
    "mode": "offline",
    "_mode_note": "'offline' = regex/extractive summary, no API key, free. 'claude' = better summaries via the Claude API; set ANTHROPIC_API_KEY in your environment and change this to 'claude'.",
    "model": "claude-haiku-4-5-20251001"
  },
  "ats": {
    "_regions_collect_note": "Which regions are collected is set in the top-level 'regions' block. It replaced ats.indiaOnly. An ATS board is global by nature - one Greenhouse board carries every office a company has - so collecting every region costs exactly the same number of requests as collecting one, while refusing them threw away 176 of the 189 live engineering internships these boards already carry.",
    "maxPostingAgeDays": 120,
    "_maxPostingAgeDays_note": "Raised from 30 to 60 because a first-party board dates a posting by when the REQUISITION was created, not when it was last advertised. Microsoft's API reports 'Applied Sciences INTERN' as 36 days old while LinkedIn was actively promoting it as 4 days old; at 30 we dropped it, along with Software Engineering INTERN (35d) and Data Science INTERN (36d). 60 admits those and still refuses the genuinely dead - Research Sciences INTERN reports 159 days. How old an ATS posting may be and still be collected. The LinkedIn scan never needed this because it searches with a time window; an ATS board has none and lists whatever is still open, so the first real poll surfaced roles 159 and 214 days old. Being first to a seven-month-old listing is not being early.",
    "_note": "Applicant-tracking-system job boards - the source LinkedIn copies from. Discovery is cached in the company_ats table by bin/discover-ats.js; bin/poll-ats.js reads it. No browser and no ToS problem: these are the public endpoints a company careers page calls to render itself."
  },
  "enrich": {
    "model": "qwen3:8b",
    "_model_note": "Ollama model tag, not a Gemini id. Must match something in `ollama list` or every call 404s.",
    "perRunLimit": 40,
    "backfillPerRun": 6,
    "_backfillPerRun_note": "How many jobs stored from card data alone get their page opened at the end of a run to fetch the description we skipped. Confidently non-tech titles are listed without being opened, which leaves description NULL - and needingEnrichment requires a description, so without this pass those rows never gain bullets, stipend or duration. Kept small and time-gated: it only uses slack left after the scan.",
    "_perRunLimit_note": "How many freshly scraped postings get sent to Gemini for bullets, eligibility and skills at the end of each run. The free tier has a daily request cap, and with runs every 30 minutes an uncapped run could spend the whole day's quota in one go. Anything skipped is picked up by the next run, because only postings with no bullets yet are ever selected.",
    "useModel": true,
    "budgetMinutes": 9,
    "_budgetMinutes_note": "Wall-clock ceiling for the whole enrichment pass. Whatever is not done is picked up next run, because only postings without bullets are ever selected."
  },
  "postQueue": {
    "_note": "The hand-picked LinkedIn post queue. An 'Add to post queue' button on each listing in the run report, then one Generate press that asks the local model for an opening line and an applying tip per posting and writes a page of finished posts to copy. NOTHING IS EVER PUBLISHED - the last step is his own hands on his own account, which is the whole reason the queue is manual.",
    "enabled": true,
    "port": 4322,
    "_port_note": "bin/queue-server.js binds this on 127.0.0.1 only. It serves the run report as well, so the buttons in it are same-origin; there are no CORS headers anywhere, which is what stops another site talking to it. 4321 is taken by web/serve.js.",
    "model": "qwen3:14b",
    "_model_note": "Ollama tag. 14b rather than the 8b the enricher uses, deliberately: the enricher runs inside a scan on a wall-clock budget and 14b measured slower AND less accurate at EXTRACTING fields, while this call is asked to WRITE and nobody is waiting on a listing. Drop to qwen3:8b if a batch feels slow.",
    "timeoutSeconds": 180,
    "utm": {
      "enabled": true,
      "source": "linkedin",
      "medium": "social",
      "_note": "Appended to interndoor.com links in a post so Vercel Analytics can separate LinkedIn traffic from organic. NEVER added to an ATS or LinkedIn apply URL - that URL belongs to somebody else and some ATS routers read the query string. Safe against the canonical: every generated page emits <link rel=\"canonical\"> pointing at the clean URL, so a tagged link cannot become a second indexed copy."
    },
    "weekly": {
      "enabled": true,
      "_regions_note": "One roundup per board, each due in its OWN time zone, all written onto a single /weekly/latest page. `region` below is the single-board fallback for an older config; `regions` is what actually runs. The week is recorded per board (weeklyRoundupWeek:<CODE>) so a quiet US week cannot mark India's as done.",
      "regions": ["IN", "US"],
      "region": "IN",
      "weekday": 0,
      "hour": 10,
      "featured": 6,
      "_featureFirst_note": "WHICH SIX ROLES GET A LINK. This is a TASTE LIST and it is meant to be edited - it decides which employers a reader recognises, which is the thing that earns the click and the one thing no signal in the data measures. Ordering is all it affects: every employer here is already through the watchlist gate and already published, so a wrong entry costs a slot, never a bad listing. Membership only, not priority - within the list the sustained footprint decides, so there is no order to maintain. An employer NOT on the list is still eligible, ranked by that same footprint, so emptying this falls back to it rather than to nothing. It was role count until 30 Aug, which measured multi-city blasting rather than the employer and put ARGMAC, Marmon Technologies India Pvt Ltd and GBJ BUZZ in the shortlist on three of four sampled weeks. Names are matched on the normalised form with a prefix either way, so HARMAN covers HARMAN India; deliberately NOT matchCompany, which matches bidirectionally and has been recorded admitting HR and India (Remote).",
      "featureFirst": [
        "Google", "Microsoft", "Amazon", "Apple", "Meta", "Nvidia", "Adobe",
        "IBM", "Intel", "AMD", "Qualcomm", "Cisco", "Oracle", "Salesforce",
        "SAP", "Dell", "Texas Instruments", "Micron Technology", "Western Digital",
        "Hewlett Packard Enterprise", "Infineon Technologies", "Samsung", "Philips",
        "Siemens", "Honeywell", "Bosch", "Nokia", "Ericsson", "Continental",
        "Stripe", "Databricks", "Cloudflare", "Palantir", "Zscaler", "Canva",
        "Notion", "Intuit", "Atlassian", "Uber", "Netflix", "Spotify", "PayPal",
        "Visa", "Mastercard", "American Express", "Goldman Sachs", "JPMorgan",
        "Morgan Stanley", "Wells Fargo", "Citi", "Barclays", "S&P Global",
        "Deloitte", "Accenture", "EY", "PwC", "KPMG", "McKinsey",
        "Flipkart", "Paytm", "Zomato", "Swiggy", "Razorpay", "Zoho", "Freshworks",
        "PhonePe", "CRED", "Groww", "Zerodha", "Meesho", "Myntra", "Nykaa",
        "Sprinklr", "Postman", "Dream11", "Ola", "InMobi", "Practo", "Urban Company",
        "Tesla", "Boeing", "Johnson & Johnson", "Epic Games", "Tower Research Capital"
      ],
      "_note": "The Sunday roundup: one post listing everything the board picked up that week. weekday is 0=Sunday. Generated by bin/weekly.js, which bin/run.sh calls after every scan - the first run at or after `hour` local time on `weekday` writes it, opens it and marks the week done in settings, so there is no second scheduler to install and a Mac that was asleep at 10:00 still gets it when it wakes.",
      "_weekday_note": "Any day works. It is Sunday because that is when someone plans the week's applications."
    }
  },
  "ollama": {
    "_note": "Local model via Ollama, on this Mac. Replaces Gemini for the scan and the enricher; the resume tailor on Vercel still uses Gemini because Vercel cannot reach this machine. No quota, so postings are sent whole and one at a time rather than truncated and batched.",
    "host": "http://127.0.0.1:11434",
    "model": "qwen3:8b",
    "_model_note": "qwen3:8b with thinking disabled. 14b was measured slower (24s vs 9s per posting) AND less accurate on eligibility, so bigger is not better here.",
    "timeoutSeconds": 120,
    "_timeoutSeconds_note": "Hard ceiling per call. A stuck model must never delay a listing - the run continues and the posting is enriched next time."
  },
  "reels": {
    "accounts": {
      "US": "interndoorusa",
      "IN": "interndoorin"
    },
    "_accounts_note": "ONE INSTAGRAM ACCOUNT PER REGION, and they must never be able to cross. @interndoorusa carries US roles, @interndoorin carries Indian ones. A reel posted to the wrong account cannot be taken back. Credentials live in .env under REGION-SUFFIXED names - IG_USER_ID_US/IG_ACCESS_TOKEN_US and IG_USER_ID_IN/IG_ACCESS_TOKEN_IN - because the bare IG_USER_ID/IG_ACCESS_TOKEN names are ALSO what storygasted's .env uses for a different account, which is the trap bin/ig_publish.py's guard was written for; with two accounts that collision stops being hypothetical. The guard still asks the live account who it is and refuses unless the username matches the region's entry here. A region absent from this map gets no reel at all, the same rule Telegram already follows for a region with no channel. Replaced the single 'account' key (was 'interndoorin') on 27 Aug.",
    "auto": {
      "enabled": true,
      "regions": [
        "IN",
        "US"
      ],
      "_regions_note": "US RESTORED 31 Aug after a read-only probe of {user-id}?fields=username returned @interndoorusa (0 followers, 25 media) instead of the OAuthException code 200 it had been answering since 28 Aug. India was probed the same way and also returned normally. The rule that put it back is the one the previous note set: probe read-only and require the ACCOUNT, never flip this to test, because a failing burst is the signal that caused the block - reelCountSince does not count `failed`, so every failure frees a cap slot and the 60-second sweep queues a replacement that fails too. THE US CAP CAME BACK AT 12, NOT 20. The block followed 30 fully-automated reels a day across two brand-new accounts from one IP, which this file's own post-mortem named as the likeliest cause; 12 + 10 is 22, still well under Instagram's documented 100/day per account, and it can be raised once both accounts have history.",
      "dailyCap": 8,
      "dailyCapByRegion": {
        "US": 8,
        "IN": 8
      },
      "perSweep": 3,
      "failureLimit": 3,
      "spacingMinutes": 180,
      "windowStartHour": 0,
      "windowEndHour": 0,
      "_cadence_note": "A REEL EVERY THREE HOURS ON EVERY AUTO ACCOUNT, AROUND THE CLOCK (31 Aug). These three settings are one decision and must move together. Before this, spacing was DERIVED as window/cap and the window was 10:00-22:00 in the region's own zone, which produced two failures at once: a nightly 13-hour silence between the last slot and the first of the next day, and - much worse - a sweep that ran overnight had every slot it handed out pushed to the window's opening, so a whole day's reels were queued at 05:00 and none could go out before 10:30. India's last reel was 29 Aug 11:42 and the next was not due until 31 Aug 10:30: 46 hours, on an account whose entire proposition is being early. windowStartHour == windowEndHour is how reelslots.js spells a 24-hour window, so intoWindow becomes a no-op and a slot is taken at the moment it is earned. spacingMinutes is now EXPLICIT rather than derived, because a derived gap silently changes whenever the cap does, and the gap is the thing being promised. 180 x 8 = 1440, so the cap below is the same decision counted the other way round - change one and change the other. The MANUAL queue keeps the 10:00-22:00 window from reels.windowStartHour: it is a button he presses, its first reel is never delayed anyway, and its audience argument is unchanged.",
      "_dailyCap_note": "EIGHT, because 24h / 3h = 8 and the cap must not be the thing that stops the cadence. It was 12 US / 10 IN under a 12-hour window. Lower per day, and deliberately: a guaranteed reel every three hours beats a burst of twelve inside twelve hours followed by silence, both for the account's own consistency and because evenly spaced posts are eight independent measurements rather than a clump. reelCountSince counts scheduled rows on a rolling 24h, so eight slots at three hours exactly fill the window and one frees every three hours as the oldest ages out - the cadence sustains itself with no further arithmetic. Still far under Instagram's documented 100/day per account.",
      "_failureLimit_note": "CIRCUIT BREAKER, added 31 Aug with the US account. Consecutive publish failures since a region's last SUCCESSFUL post, after which automatic queueing pauses for that region. It exists because a failure is invisible to the daily cap - reelCountSince counts rendering|scheduled|publishing|published and NOT failed - so a blocked endpoint frees a slot, the 60-second sweep refills it, and that fails too. On 28-29 Aug that loop turned a cap of 20 into 36 failed US reels while Instagram answered 'API access blocked', and hammering an endpoint that is blocking you is the worst thing to be doing while an app restriction is live. It has been stopped by hand twice. Three, not one: a single failure is as likely to be a tunnel that did not come up as an account problem, and retrying that is correct. Nothing has to be reset - one successful post clears it, which is also why the count is measured since the last success rather than over a flat window. A row cancelled by hand is NOT a failure and cannot trip it; see reelFailuresSinceSuccess for the discriminator.",
      "_maxAgeHours_note": "A WEEK, not the original 48h. India intake fell to ~2 tech rows a day, so at 48h the eligible pool was literally ZERO after one-role-one-reel dedupe and the account went quiet while the daily cap sat at 3 of 10. Measured on the live board: 48h and 72h yield 0 eligible roles, 96h yields 1, 120h yields 4, and 168h yields 33 - the step where the pre-collapse intake comes into range. Not widened further: the median unreeled India row is 14 days old, and a fortnight-old posting is not what an account built on being early should be showing. Safe because the voiceover makes no freshness claim, the card stamps the real age on every frame, and a week is well inside the 30-day page retention. The SCARCITY line is gated separately - see applicants() in bin/render-reel.js - because an applicant count frozen a week ago must never become \"nobody has applied yet\".",
      "maxAgeHours": 168
    },
    "_auto_note": "Publish a reel for newly scraped jobs automatically, with no approval step. THE CAP IS FORCED BY THE PLATFORM, not by taste: Instagram's Content Publishing API allows 100 posts per rolling 24h per account, verified live, and US intake over the fortnight to 27 Aug ran mean 78 tech listings a day, MEDIAN 105 and peak 138. So 'a reel for every new US job' is not something the account is permitted to do - on a normal day the API would start refusing partway through, and the reels lost would be an arbitrary tail rather than the least interesting ones. An explicit cap means the pipeline chooses which roles go out, freshest first. Raise dailyCap when the account has history: a profile with one post jumping to 100/day is the shape Instagram restricts. Spacing is DERIVED from the cap (window hours / cap) rather than taken from spacingMinutes, which is 180 and belongs to the manual queue - at 180 the 10:00-22:00 window holds four posts, so a cap of 20 would take three days to deliver one day's roles. Set auto.spacingMinutes to override. enabled:false stops all automatic posting without unpicking the config; a region listed here but absent from 'accounts' is skipped.",
    "_account_note": "The Instagram USERNAME that must own IG_ACCESS_TOKEN. bin/ig_publish.py asks the live account who it is and refuses to publish unless this matches, because storygasted's .env holds credentials for a DIFFERENT account (storygasted) under the same variable names - and a reel posted to the wrong account cannot be taken back. It is 'interndoorin', not 'interndoor': the Instagram handle and the Telegram channel are not the same string.",
    "voiceTempo": 1.15,
    "_voiceTempo_note": "How much the voiceover is sped up after synthesis. Raw Qwen3-TTS runs about 1.7 words/second. Settled by listening, over several rounds: 1.25 too slow, 1.5 fine, 1.6 a little too fast, 1.45 too fast, 1.3 too fast. TWO THINGS MOVED THE TARGET DOWN and both are about pauses rather than word rate - src/reelscript.js builds one clause per beat instead of four sentences, and bin/render-reel.js synthesises each beat separately and joins them with beatPause. Both removed dead air, and dead air is what made 1.25 feel slow in the first place, so the right tempo ended up BELOW the 1.5 that once felt right rather than above it. Do not re-derive this from words-per-second; it is a felt quantity. REEL_VO_TEMPO=1.05 npm run reel -- --job=<id> compares without editing anything.",
    "spacingMinutes": 180,
    "windowStartHour": 10,
    "windowEndHour": 22,
    "timeZone": "Asia/Kolkata",
    "_spacing_note": "Three good jobs found in one sitting become three posts, not one burst - not because Instagram punishes bursts (it does not, and at zero followers there is no follower feed for them to compete in) but because three reels dropped into one slot is ONE measurement, not three, and measurement is what the bandit will read. THE FIRST REEL OF A SITTING IS NEVER DELAYED: he pressed the button because he wants it out. windowStartHour/windowEndHour keep slots off the small hours - the board's own intake data puts weekday attention in the 16:00-20:00 IST block against almost nothing at 08:00-12:00, so a reel scheduled for 03:00 is a wasted reel however well spaced. Set start == end for a 24-hour window.",
    "voiceInstruct": "An upbeat young presenter announcing a job opening to students. Bright, confident and energetic, with the quick forward pace of a short social promo. Keep the momentum up throughout, do not dwell on any word, and take no long pauses.",
    "_voiceInstruct_note": "The delivery direction handed to Qwen3-TTS, and it matters as much as voiceTempo. WITHOUT ONE, storygasted's own default applies - 'a young adult American woman telling a personal story to a close friend, natural conversational pacing, warm and intimate, NEVER ANNOUNCER-LIKE' - which is a storytelling voice and exactly the unhurried delivery a job promo does not want. Every reel inherited it until this was passed. Pace also comes from the SCRIPT: see src/reelscript.js, which builds one running sentence rather than four, because every full stop is a pause the model inserts.",
    "minSeconds": 10,
    "_minSeconds_note": "Shortest reel worth making. The card lays its scenes out as FRACTIONS of the total, so a short voiceover squeezes every scene - at 1.6x a brief posting ran 5.8s, which gave the CTA 0.9 seconds. That is not long enough to read INTERNDOOR.COM, let alone type it, and the CTA is the only scene that asks the viewer to do anything. The tail is padded to reach this rather than the voice being slowed back down: the pace was the complaint, and a beat of music under the CTA is not dead air, it is the pause that lets the address land.",
    "beatPause": 0.22,
    "_beatPause_note": "Silence inserted between the script's beats when they are synthesised separately. ONE LONG UTTERANCE WAS THE PROBLEM: in isolation the CTA line synthesises in 2.40s with no internal gaps at all, but inside the full script the same words came out as 'on ... InternDoor ... dot ... com' with 0.77s, 0.25s and 0.21s of silence in them, and a 1.35s hole before 'Find'. The model's pacing drifts over a long stretch and voiceInstruct does not hold it. Each line is now synthesised on its own and joined with this pause, so the gap is chosen rather than whatever the model felt like. storygasted does the same thing (its beat_pause is 0.45, longer because it is telling a story)."
  }
}
