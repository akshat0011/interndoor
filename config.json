{
  "_comment": "Edit this file to control what gets watched. Everything else has sane defaults.",
  "companiesFile": "companies.json",
  "_companiesFile_note": "The main watchlist, ~900 companies grouped by sector. Delete whole groups from that file to narrow coverage. Anything added to the inline 'companies' list below is merged on top of it.",
  "_companies_note": "Your own additions go here - they are merged with companiesFile.",
  "companies": [
    {
      "name": "Dream11",
      "aliases": [
        "Dream Sports"
      ]
    },
    {
      "name": "EXL",
      "aliases": [
        "EXL Service",
        "ExlService",
        "EXL Analytics"
      ]
    },
    {
      "name": "GE HealthCare",
      "aliases": [
        "GE Healthcare"
      ]
    },
    {
      "name": "Bybit"
    },
    {
      "name": "CertifyOS"
    },
    {
      "name": "Tractebel",
      "aliases": [
        "Tractebel Engineering",
        "Tractebel India"
      ]
    },
    {
      "name": "1Fi",
      "aliases": [
        "1Finance",
        "OneFi"
      ]
    },
    {
      "name": "Joveo"
    },
    {
      "name": "Quicket Solutions"
    },
    {
      "name": "BSI Financial Services",
      "aliases": [
        "BSI Financial"
      ]
    },
    {
      "name": "HPE",
      "aliases": [
        "Hewlett Packard Enterprise",
        "Hewlett-Packard Enterprise"
      ]
    },
    {
      "name": "GE Vernova"
    },
    {
      "name": "PwC",
      "aliases": [
        "PwC Acceleration Center",
        "PricewaterhouseCoopers"
      ]
    },
    {
      "name": "CoinDCX",
      "aliases": [
        "CoinDCX Careers",
        "Neblio Technologies"
      ]
    },
    {
      "name": "Flam",
      "aliases": [
        "FlamAI",
        "Flam AI"
      ]
    },
    {
      "name": "Janitri"
    },
    {
      "name": "Convin"
    },
    {
      "name": "Constraal"
    },
    {
      "name": "Branch International",
      "aliases": [
        "Branch Personal Finance",
        "Branch Metrics India"
      ]
    },
    {
      "name": "Marmon Technologies India",
      "aliases": [
        "Marmon Technologies",
        "Marmon Holdings",
        "Marmon Group"
      ]
    },
    {
      "name": "Gallagher",
      "aliases": [
        "Arthur J. Gallagher",
        "AJG Global"
      ]
    },
    {
      "name": "SLB",
      "aliases": [
        "Schlumberger",
        "SLB India",
        "Schlumberger Limited"
      ]
    },
    {
      "name": "Criteo"
    },
    {
      "name": "Oceaneering"
    },
    {
      "name": "Monotype"
    },
    {
      "name": "Pearson",
      "aliases": [
        "Pearson India"
      ]
    },
    {
      "name": "Enverus"
    },
    {
      "name": "Livspace"
    },
    {
      "name": "Collegedunia"
    },
    {
      "name": "V-Mart Retail",
      "aliases": [
        "V-Mart"
      ]
    },
    {
      "name": "TeamLease Regtech",
      "aliases": [
        "TeamLease"
      ]
    },
    {
      "name": "Bizlem"
    },
    {
      "name": "Bugsmirror"
    },
    {
      "name": "SmartQ",
      "aliases": [
        "Smart Q"
      ]
    },
    {
      "name": "Nippon Data Systems"
    },
    {
      "name": "Picktime"
    },
    {
      "name": "Aerocraft Engineering"
    },
    {
      "name": "AppVersal"
    },
    {
      "name": "Menstrupedia"
    },
    {
      "name": "MiM-Essay"
    },
    {
      "name": "unbundl"
    },
    {
      "name": "National Technology Centre for Ports, Waterways and Coasts",
      "aliases": [
        "NTCPWC"
      ]
    },
    {
      "name": "SurveySparrow",
      "aliases": [
        "Survey Sparrow"
      ]
    },
    {
      "name": "Zycus"
    },
    {
      "name": "Chegg India",
      "aliases": [
        "Chegg"
      ]
    },
    {
      "name": "NielsenIQ",
      "aliases": [
        "Nielsen IQ",
        "NIQ"
      ]
    },
    {
      "name": "Fanatics"
    },
    {
      "name": "Exabeam",
      "aliases": [
        "LogRhythm"
      ]
    },
    {
      "name": "TUV SUD",
      "aliases": [
        "TÜV SÜD",
        "TUV SUD South Asia",
        "TUV SUD India"
      ]
    },
    {
      "name": "Avendus",
      "aliases": [
        "Avendus Capital"
      ]
    },
    {
      "name": "YASH Technologies",
      "aliases": [
        "YASH Technologies Pvt Ltd"
      ]
    },
    {
      "name": "Qualitrol",
      "aliases": [
        "Qualitrol Company"
      ]
    },
    {
      "name": "GoKwik",
      "aliases": [
        "Go Kwik"
      ]
    },
    {
      "name": "CashFlo"
    },
    {
      "name": "Sanas",
      "aliases": [
        "Sanas AI"
      ]
    },
    {
      "name": "GreedyGame",
      "aliases": [
        "Greedy Game"
      ]
    },
    {
      "name": "InfiVR",
      "aliases": [
        "Infi VR",
        "Infinite Reality Labs",
        "Infi AR/VR"
      ]
    },
    {
      "name": "STEMpedia",
      "aliases": [
        "Agilo Research"
      ]
    },
    {
      "name": "International Council on Clean Transportation",
      "aliases": [
        "ICCT"
      ]
    },
    {
      "name": "D Square Consulting Services Pvt Ltd",
      "aliases": [
        "D Square Consulting Services",
        "DSquare",
        "D Square Analytix"
      ]
    },
    {
      "name": "Subspace",
      "aliases": [
        "Grow90",
        "Subspace Money",
        "Whatsub"
      ]
    },
    {
      "name": "Lexsi Labs",
      "aliases": [
        "Lexsi.ai",
        "AryaXAI",
        "AryaXAI Alignment Labs",
        "Arya.ai"
      ]
    },
    {
      "name": "TurboHire",
      "aliases": [
        "TurboHire Technologies",
        "Turbo Hire Technologies Pte Ltd"
      ]
    },
    {
      "name": "Nuvama Wealth",
      "aliases": [
        "Nuvama Wealth Management",
        "Nuvama Private",
        "Nuvama Wealth and Investment",
        "Edelweiss Wealth Management"
      ]
    },
    {
      "name": "ALLEN Digital",
      "aliases": [
        "ALLEN Career Institute",
        "ALLEN",
        "Allen Digital Learning"
      ]
    },
    {
      "name": "SolarSquare",
      "aliases": [
        "SolarSquare Energy",
        "SolarSquare Energy Private Limited"
      ]
    },
    {
      "name": "Bristlecone",
      "aliases": [
        "Bristlecone India Limited",
        "Mahindra Bristlecone",
        "BristleconeLabs.ai"
      ]
    },
    {
      "name": "DigiPlus IT",
      "aliases": [
        "DigiPlus",
        "digiplus.co.in"
      ]
    },
    {
      "name": "Snackible",
      "aliases": [
        "LightSaber Food Ventures",
        "LightSaber Food Ventures Pvt Ltd"
      ]
    },
    {
      "name": "tech.at.core",
      "aliases": [
        "techatcore",
        "tech at core",
        "techatcore.com"
      ]
    },
    {
      "name": "FynTune Solution Private Limited",
      "aliases": [
        "FynTune",
        "Fyntune Solutions",
        "fyntune.com"
      ]
    },
    {
      "name": "WATRANA TRACTION PRIVATE LIMITED",
      "aliases": [
        "Watrana",
        "Watrana Traction Company",
        "watrana.com"
      ]
    },
    {
      "name": "ARGMAC",
      "aliases": [
        "Argmac Luxury",
        "Grinder Inc (IndiaMART seller name)",
        "argmac.com"
      ]
    },
    {
      "name": "HBeonLabs Technologies Private Limited",
      "aliases": [
        "Hbeonlabs",
        "HBeon Labs",
        "Hbeonlabs Technologies Pvt Ltd"
      ]
    },
    {
      "name": "METNMAT Research & Innovations",
      "aliases": [
        "Metnmat",
        "Metnmat Innovations Private Limited",
        "Metnmat Research and Development"
      ]
    },
    {
      "name": "LUXASIA",
      "aliases": [
        "LUXASIA Group",
        "L Beauty",
        "Luxasia Cosmetics India Private Limited"
      ]
    },
    {
      "name": "Intouch Quality Services Private Limited",
      "aliases": [
        "Intouch Group",
        "Intouch Quality Services Pvt Ltd",
        "intouchgroup.in"
      ]
    },
    {
      "name": "CAST",
      "aliases": [
        "CAST Software",
        "CAST Software India Private Limited",
        "CAST Highlight"
      ]
    },
    {
      "name": "Adfluence Hub",
      "aliases": [
        "AdfluenceHub",
        "adfluencehub"
      ]
    },
    {
      "name": "JSW Group",
      "aliases": [
        "JSW Steel",
        "JSW Energy"
      ]
    },
    {
      "name": "Dun & Bradstreet",
      "aliases": [
        "Dun and Bradstreet"
      ]
    },
    {
      "name": "Celanese"
    },
    {
      "name": "Copeland"
    },
    {
      "name": "IQVIA"
    },
    {
      "name": "Dana Incorporated",
      "aliases": [
        "Dana India"
      ]
    },
    {
      "name": "AtkinsRéalis",
      "aliases": [
        "AtkinsRealis",
        "Atkins Realis",
        "SNC-Lavalin"
      ]
    },
    {
      "name": "General Mills"
    },
    {
      "name": "Mondelēz International",
      "aliases": [
        "Mondelez International",
        "Mondelez"
      ]
    },
    {
      "name": "GlobalLogic"
    },
    {
      "name": "Visteon",
      "aliases": [
        "Visteon Corporation"
      ]
    },
    {
      "name": "Legrand"
    },
    {
      "name": "Flex",
      "aliases": [
        "Flextronics"
      ]
    },
    {
      "name": "Festo",
      "aliases": [
        "Festo India"
      ]
    },
    {
      "name": "FORVIA HELLA",
      "aliases": [
        "Forvia",
        "HELLA"
      ]
    },
    {
      "name": "Glencore"
    },
    {
      "name": "Wolters Kluwer"
    },
    {
      "name": "ICON plc",
      "aliases": [
        "ICON Clinical Research"
      ]
    },
    {
      "name": "Conduent"
    },
    {
      "name": "PTC",
      "aliases": [
        "PTC Inc",
        "Parametric Technology"
      ]
    },
    {
      "name": "West Pharmaceutical Services",
      "aliases": [
        "West Pharma"
      ]
    },
    {
      "name": "Avient",
      "aliases": [
        "Avient Corporation"
      ]
    },
    {
      "name": "NKT"
    },
    {
      "name": "PolyPeptide",
      "aliases": [
        "PolyPeptide Group"
      ]
    },
    {
      "name": "IFF",
      "aliases": [
        "International Flavors & Fragrances",
        "International Flavors and Fragrances"
      ]
    },
    {
      "name": "Crane NXT",
      "aliases": [
        "Crane Authentication",
        "Crane Currency"
      ]
    },
    {
      "name": "Calix"
    },
    {
      "name": "Veradigm"
    },
    {
      "name": "Everllence",
      "aliases": [
        "MAN Energy Solutions"
      ]
    },
    {
      "name": "Hill International"
    },
    {
      "name": "Hines"
    },
    {
      "name": "Russell Investments"
    },
    {
      "name": "Baker Tilly"
    },
    {
      "name": "CohnReznick"
    },
    {
      "name": "McDonald's",
      "aliases": [
        "McDonalds",
        "McDonald's Global Office in India"
      ]
    },
    {
      "name": "dentsu"
    },
    {
      "name": "Havas",
      "aliases": [
        "Havas Media Network India",
        "Havas Creative Network India"
      ]
    },
    {
      "name": "Teleperformance",
      "aliases": [
        "TP"
      ]
    },
    {
      "name": "Numerator"
    },
    {
      "name": "UST",
      "aliases": [
        "UST Global"
      ]
    },
    {
      "name": "AML RightSource"
    },
    {
      "name": "Etraveli Group"
    },
    {
      "name": "Air Arabia"
    },
    {
      "name": "Freight Tiger"
    },
    {
      "name": "FanCode"
    },
    {
      "name": "Enterpret"
    },
    {
      "name": "BiteSpeed"
    },
    {
      "name": "Hevo Data",
      "aliases": [
        "Hevo"
      ]
    },
    {
      "name": "HighLevel"
    },
    {
      "name": "Unicommerce"
    },
    {
      "name": "Kapture CX",
      "aliases": [
        "Kapture"
      ]
    },
    {
      "name": "SiteRecon"
    },
    {
      "name": "Snapmint"
    },
    {
      "name": "Wint Wealth"
    },
    {
      "name": "KYC Hub"
    },
    {
      "name": "Dodo Payments"
    },
    {
      "name": "API Holdings",
      "aliases": [
        "PharmEasy"
      ]
    },
    {
      "name": "Honasa Consumer",
      "aliases": [
        "Honasa",
        "Mamaearth"
      ]
    },
    {
      "name": "Kapiva"
    },
    {
      "name": "Blissclub"
    },
    {
      "name": "Plix"
    },
    {
      "name": "Mosaic Wellness"
    },
    {
      "name": "Zolo"
    },
    {
      "name": "WeWork India",
      "aliases": [
        "WeWork"
      ]
    },
    {
      "name": "Pickyourtrail"
    },
    {
      "name": "WedMeGood"
    },
    {
      "name": "Rusk Media"
    },
    {
      "name": "Vecmocon",
      "aliases": [
        "Vecmocon Technologies"
      ]
    },
    {
      "name": "HaystackAnalytics"
    },
    {
      "name": "Medanta"
    },
    {
      "name": "Trilegal"
    },
    {
      "name": "Ujjivan Small Finance Bank",
      "aliases": [
        "Ujjivan"
      ]
    },
    {
      "name": "Resollect"
    },
    {
      "name": "Teli Labs"
    },
    {
      "name": "Fynd"
    },
    {
      "name": "AppsForBharat"
    },
    {
      "name": "CredFlow"
    },
    {
      "name": "Datacultr"
    },
    {
      "name": "Volody"
    },
    {
      "name": "LineupX"
    },
    {
      "name": "V2 Retail",
      "aliases": [
        "V2 Retail Ltd",
        "Vishal Retail"
      ]
    }
  ],
  "_acronym_alias_note": "A watchlist term under 4 characters can only ever match EXACTLY, because matchCompany refuses to substring-match short terms (that guard is what stops 'Navi' matching every company with Navi Mumbai in its name). So an acronym-only entry never fires against the full name LinkedIn actually shows. 'HPE' was on the list the whole time while 'Hewlett Packard Enterprise' was dropped 10 times as off-watchlist. Same for GE Vernova, PwC Acceleration Center India and JSW Steel. If you add an acronym, add the spelled-out name as an alias too.",
  "_added_from_linkedin_note": "Employers first noticed in LinkedIn's own recommendations, each posting a real engineering internship the watchlist was turning away. Added by hand on purpose. A broad sweep is not a shortcut here: the off-watchlist employers posting the MOST engineering internships are almost entirely training schemes and staffing spam - MediNex Workforce alone posted 34, and 'Future Interns', 'kaushalsaathi' and 'EazyByts' follow it. Genuine companies post once or twice, so frequency ranks the junk to the top. Remove one with `npm run remove -- \"<name>\"`.",
  "searchMode": "keywords",
  "_searchMode_note": "'keywords' (default) = run the searches list below; with a single broad entry that means one sweep paginated to exhaustion, filtered locally. 'companies' = boolean OR batches of the watchlist - NOT recommended: LinkedIn treats those names as loose text and returns mostly unrelated employers. 'both' = company batches then the searches list.",
  "companySearch": {
    "batchSize": 30,
    "_batchSize_note": "Companies per query. Batches are also capped by URL length, so a group of long names may hold fewer than this.",
    "extraKeywords": "",
    "_extraKeywords_note": "Prepended to every batch query. Deliberately empty: filters.jobTypes already restricts results to internships, and adding the literal word 'internship' would ALSO require it to appear in the job text - excluding postings titled e.g. 'SDE Intern' that never spell it out. Set it only if you want that narrowing."
  },
  "defaultLocation": "India",
  "_defaultLocation_note": "Applied to any search written as a plain string. Change this one value to move the whole watch to another city or country.",
  "_searches_note": "One broad search PER REGION, paginated to exhaustion, then filtered locally by title, role and company. The boolean form matters: a plain 'internship' keyword misses postings titled only 'Intern' (Salesforce's 'Summer 2027 Intern - Software Engineer' is one), and the OR list covers every phrasing the title filter accepts. Keep it to one entry per region - each extra search costs a full pagination pass against the single account everything depends on, so add a region only when its board is worth the requests, and see limits.searchesPerRun for how to make regions take turns instead.",
  "_searches_region_note": "'region' is what a card's location falls back to when LinkedIn renders none, and it is also the key each region's own lookback window and covered-ground baseline are stored under. 'geoId' is what actually scopes the results: the redirect to /jobs/search-results/ DROPS 'location=' but keeps 'geoId', so the location string is cosmetic and the id is load-bearing. India's entry deliberately carries NO geoId - it has always come back Indian from account geo bias alone, and sending an id would change what LinkedIn returns on the collector that supplies 91% of the India board. The ids live in src/regions.js.",
  "searches": [
    {
      "keywords": "(intern OR internship OR trainee OR \"co-op\" OR apprentice)",
      "region": "IN"
    },
    {
      "keywords": "(intern OR internship OR trainee OR \"co-op\" OR apprentice)",
      "region": "US",
      "location": "United States",
      "geoId": 103644278,
      "intervalMinutes": 60
    }
  ],
  "_searches_interval_note": "'intervalMinutes' makes ONE search run less often than the loop ticks, which is different from limits.searchesPerRun - that dial makes every region take turns at the same rate, this one sets a rate per region. India has none, so it is swept every 30-minute tick and keeps the freshness the board promises. The US runs hourly: its supply is dense and steady all day (24 intern-titled cards in a 12-minute window when measured), so half the sweeps buys most of the roles at half the page loads against the one account everything depends on. Skipping is not failing - the region's baseline is left alone, so the next sweep's adaptive window has stretched to cover the gap and nothing is missed.",
  "filters": {
    "adaptiveWindow": true,
    "_adaptiveWindow_note": "Sizes the lookback from how long ago the last successful run was, instead of using one fixed number. With hourly runs a fixed 30-hour window would re-paginate 30 hours of postings every time to find the newest hour - roughly 30x the page loads for nothing. After a normal hourly run the window is ~3h; after an overnight sleep it stretches to cover the gap, so nothing is missed.",
    "minWindowHours": 3,
    "maxWindowHours": 36,
    "postedWithinHours": 30,
    "_postedWithinHours_note": "Used only when adaptiveWindow is false, or when there is no previous run to measure from.",
    "sortBy": "recent",
    "jobTypes": [],
    "_jobTypes_note": "DELIBERATELY EMPTY. Setting this to ['INTERNSHIP'] sends f_JT=I, which trusts the recruiter's employment-type tag - and they get it wrong constantly. A real case: Salesforce's 'Summer 2027 Intern - Software Engineer' is tagged Full-time, so f_JT=I hid it completely. The title filter and role classifier below decide what is an internship instead, from the title itself. LinkedIn f_JT codes if you ever want them: F=full-time P=part-time C=contract T=temporary I=internship V=volunteer O=other."
  },
  "roleClassifier": {
    "_useGemini_note": "Currently OFF - the offline vocabulary in src/roles.js decides. Gemini was never the speed bottleneck (it runs as one batch after the walk, not per job), but the offline classifier is instant and free, so there is little reason to switch it on unless the vocabulary starts missing things. When true: ask Gemini whether each captured role is a software role. Needs GEMINI_API_KEY in the environment - put it in a .env file at the project root (gitignored) and it is picked up automatically. Titles are sent as ONE batched request per run, not one per job. If the key is missing, the quota is spent, or the call fails for any reason, the offline vocabulary in src/roles.js decides instead - a job is never left unclassified because an API was down.",
    "_useGeminiForAmbiguous_note": "The one place Gemini still earns its keep. A title the vocabulary cannot settle - a bare 'Trainee', 'Apprentice', or something resting only on the word 'Engineer' - gets its DESCRIPTION read instead. Gemini also names the phrase its decision turned on, and that phrase is added to the offline vocabulary, so the same kind of title is answered instantly and free from then on. The vocabulary therefore gets better and the API gets called less the longer this runs. Vetted before anything is learned: the phrase must actually appear in the posting, must not contradict a built-in rule, and must be a sane length.",
    "model": "qwen3:8b",
    "useModel": true,
    "useModelForAmbiguous": true,
    "_model_note": "Ollama model tag. Same model as the enricher so only one stays resident in memory."
  },
  "matching": {
    "storeNonTechRoles": false,
    "_storeNonTechRoles_note": "false = a confidently non-engineering title is discarded during the sweep instead of being stored. The site is engineering-only, so storing them meant carrying ~80% of rows that could never be published, and spending description backfills on them. They are still recorded in seen_cards so the next run does not re-decide the same card. Set true to collect them again (and set publish.techRolesOnly false to show them).",
    "openNonTechRoles": false,
    "_openNonTechRoles_note": "false = a role the classifier CONFIDENTLY calls non-tech is listed from its search-card data alone, with no page open. Ambiguous titles are still opened, since that is exactly when the description decides. On a real backfill this cut opens from 12 to 4 per dozen candidates. Card-only listings have no description, summary or skills, which is fine for the Other roles tab. Set true to open everything.",
    "skipViewedCards": false,
    "_skipViewedCards_note": "OFF. LinkedIn marks a card Viewed whether the scraper opened it or you did, so browsing LinkedIn yourself made those postings permanently invisible to your own site - 93 watchlist listings were lost that way, including GE HealthCare, Citi, Honeywell, Times Internet and Infineon. The re-opening it was meant to prevent is already prevented by seen_cards and the run lock, so the protection was redundant and the cost was real. Set true only if runs start re-opening cards they have already processed.",
    "requireCompanyMatch": true,
    "_requireCompanyMatch_note": "true = the FIRST gate. A posting from a company not on the watchlist is skipped outright - no title parsing, no role classification, no Gemini call. Set false to consider every internship found, which is far more work and spends classifier quota on employers you do not care about.",
    "titleMustMatch": [
      "intern",
      "internship",
      "trainee",
      "co-op",
      "coop",
      "summer analyst",
      "apprentice"
    ],
    "_titleMustMatch_note": "A job must contain one of these in its title. Use [] to disable.",
    "_roles_note": "Engineering roles only. A confidently non-technical title is discarded during the sweep (storeNonTechRoles) and never published (publish.techRolesOnly). An AMBIGUOUS title is still opened and judged on its description - that is exactly the case the title cannot settle, and dropping those on the title alone would lose real engineering roles posted as a bare 'Trainee'.",
    "extraTechTerms": [],
    "extraNonTechTerms": [
      "financial",
      "indirect tax",
      "technical support",
      "support representative",
      "credit risk",
      "fraud risk",
      "market risk",
      "credit analyst",
      "treasury",
      "financial crime",
      "anti-money laundering",
      "regulatory compliance",
      "compliance analyst",
      "underwriting",
      "actuarial"
    ],
    "_extraNonTechTerms_note": "Matching is whole-word, so the built-in 'finance' never matched 'Financial Analyst' and 'taxation' never matched 'Indirect Tax'. Those fell through as uncertain, and since an uncertain title is now published rather than held, six Amazon finance roles would have gone onto an engineering-only site. Remove 'financial' if a title like 'Financial Systems Software Engineer' ever gets wrongly dropped.",
    "_riskTerms_note": "The risk/compliance block was added 16 Aug 2026 after 'Apprentice - Credit Risk Analyst' at American Express went live on an engineering board. Amex posts EVERY internship as the bare word 'Apprentice', so the title can never be settled and the description decides; the model called it tech because the prompt counts 'data science and analytics' as tech and the role does run on SQL, Python and Tableau. These terms are matched against the model's own role label as well as the title (see vetoNonTech). They are safe against genuine engineering roles inside risk teams: a negative only wins when no MULTI-WORD positive also matches, so 'Credit Risk Data Engineer' still classifies tech via 'data engineer'. Every term names a FINANCE domain on purpose. A bare 'risk analyst' / 'risk management' was tried first and dropped: it flipped Zscaler's 'Insider Risk Analyst', which is security operations and squarely in scope. The domain-specific terms already caught every bad row, so the broad ones bought nothing and cost a real listing.",
    "_extraTerms_note": "Your own additions to the classifier, checked ahead of the built-in lists.",
    "blocklist": [
      "MedTourEasy",
      "Silicon Global Consulting"
    ],
    "_blocklist_note": "Employers never published, whatever the watchlist says. Checked before any watchlist term, so a fuzzy match cannot resurrect one. Matching is containment on the normalised name, so \"MedTourEasy\" also covers \"MedTourEasy Navi Mumbai\". MedTourEasy is here because it was reported as a scam; it only ever reached the site because \"MedTourEasy Navi Mumbai\" contains the city Navi Mumbai, which the matcher read as the fintech Navi."
  },
  "pacing": {
    "_note": "All values in milliseconds, randomised per action. These were roughly halved for speed, which roughly doubles the request rate - a real increase in ban risk. If LinkedIn ever rate-limits, raise these first; the 24h cooldown will have already stopped the run.",
    "betweenCards": [
      1800,
      3600
    ],
    "betweenPages": [
      4000,
      8000
    ],
    "betweenSearches": [
      20000,
      40000
    ],
    "scrollStep": [
      260,
      700
    ],
    "afterNavigation": [
      1500,
      3000
    ],
    "warmupOnFeed": [
      3000,
      6000
    ],
    "longBreakEvery": 30,
    "longBreak": [
      12000,
      25000
    ],
    "_longBreak_note": "Every N job cards, take a longer human-style break.",
    "startupJitter": [
      0,
      60000
    ],
    "_startupJitter_note": "Scheduled runs wait a random extra 0-60s, so activity does not land at exactly HH:00:00 every time. Was 0-4 min when runs were half-hourly; at 15-minute slots that much jitter could push a run most of the way into the next slot, where the lock would then skip it."
  },
  "limits": {
    "maxRuntimeMinutes": 90,
    "maxPagesPerSearch": 40,
    "maxDetailsPerRun": 100,
    "searchesPerRun": 0,
    "_searchesPerRun_note": "How many of the searches above one run may walk. 0 (the default) means all of them, so every region is swept every tick. Set it to 1 to make the regions TAKE TURNS: the rotation cursor already remembers where the last run stopped, so nothing is skipped - each region is simply swept half as often and total request volume stays exactly where it was on a single region. That matters because LinkedIn dropped this session twice in August without ever answering 429, and page loads are the budget a rate limit is eventually spent on. Raise the region count and this is the first dial to reach for.",
    "_maxRuntimeMinutes_note": "There is no schedule slot to overrun any more, so this no longer has to stay under the interval. bin/loop.sh runs one scan at a time and starts the next when the previous one finishes, so a long run costs nothing but its own time - it can never collide with, or be skipped by, another run. Raised from 20 to 90 so a scan is not cut off mid-pagination: the budget is a backstop against a genuinely stuck run, not the normal stopping point. Paging normally ends when LinkedIn's Next button runs out.",
    "_note": "Safety backstops, not the normal stopping point. Paging normally ends when LinkedIn's 'Next' button runs out, which means every job in the window has been seen. These caps only bite if a search is unusually deep - and when one does bite, the report says so explicitly rather than pretending the search finished.",
    "_maxPagesPerSearch_note": "40 pages x 25 = 1000 results, which is roughly LinkedIn's own ceiling for a single search. With the 24-hour filter on, most searches end far sooner."
  },
  "notifications": {
    "onCaptcha": true,
    "onNewJobs": true,
    "onError": true,
    "openReportWhenDone": true,
    "_openReportWhenDone_note": "Auto-open the HTML report in your default browser when new jobs are found.",
    "telegram": {
      "enabled": true,
      "chatId": "@interndoor",
      "channels": { "IN": "@interndoor", "US": "@interndoorusa" },
      "_channels_ids_note": "Numeric ids, for recovery if a channel is ever renamed: IN @interndoor = -1004300938042, US @interndoorusa = -1004479304597. The usernames are used above because they are readable, but a rename breaks them and the numeric id cannot change - this project renamed its brand twice in one day, so keep these written down. Verified 25 Aug: @Internzobot is an administrator of both with can_post_messages true. GB has no channel and therefore gets no post.",
      "_channels_note": "One channel per region, keyed by ISO code. A region with no entry here gets NO post - posting US listings to people who subscribed for internships in India is how a channel gets muted. chatId above predates regions and is kept as India's fallback, so an empty channels map behaves exactly as before. Only published regions are posted at all: a region that is collected but not published has no pages written for it, so every link would 404.",
      "_note": "Posts one message per run listing the new roles, each linking to its page on the site. One message rather than one per job: a run that finds six roles firing six notifications is how a channel gets muted. Set enabled to true and chatId to your channel (\"@yourchannel\", or the numeric -100... id for a private one), and put TELEGRAM_BOT_TOKEN in .env. The bot must be an administrator of the channel with permission to post, or Telegram returns 403 and the post is skipped. Everything here fails soft - a channel that is misconfigured, rate-limited or down never fails the scrape."
    }
  },
  "browser": {
    "headed": true,
    "_headed_note": "Must stay true. Headless Chromium reports 'HeadlessChrome' in its user agent and an 800x600 screen - trivially detectable, and you could not solve a CAPTCHA in a window you cannot see. The tool refuses to start if this is false.",
    "windowSize": [
      1512,
      950
    ],
    "windowPosition": [
      40,
      40
    ],
    "_windowPosition_note": "Where the Brave window opens. Set to [-2400, -2400] to park it off-screen if you find it distracting - but then you would have to drag it back into view to solve a CAPTCHA.",
    "returnFocus": true,
    "_returnFocus_note": "Brave always steals keyboard focus when it launches. This hands focus straight back to whatever app you were using, so a run at 12:00 does not interrupt you.",
    "disableBraveShields": true,
    "_disableBraveShields_note": "Passes --disable-brave-extension. Mostly belt-and-braces: on a fresh profile Shields has no filter lists to work from, and LinkedIn was verified to load fine either way."
  },
  "regions": {
    "_note": "Where postings are collected from, and where they are shown. Collecting a region and publishing one are separate settings on purpose: collection is cheap and reversible - an ATS board is read once whatever its rows turn out to be - while publishing a region is a public commitment to a board that has to look worth visiting. Keeping them apart is what lets a region fill up quietly for a month before anyone is shown it.",
    "collect": "all",
    "_collect_note": "'all' (default) collects every region src/regions.js knows, plus 'unknown'. A location the gazetteer cannot read is STORED and never published, so a better gazetteer reads it later without re-collecting anything. An array of ISO codes narrows it, e.g. [\"IN\",\"US\"].",
    "publish": ["IN", "US", "GB"],
    "_publish_note": "ISO codes, in the order the region switch lists them. India is served at the site ROOT and every other region at /<slug>/ - the slugs are in src/regions.js, and GB is served at /uk/. Add a code here only once that region's board holds enough live roles to be worth a visit: node -e \"import('./src/store.js').then(m=>console.log(new m.Store().regionCounts({sinceMs:Date.now()-14*86400000})))\"",
    "_urls_note": "India stays at the ROOT permanently. Its ~130 job pages and 125 company hubs are already indexed at /jobs/... and /companies/...; moving them under /in/ to make the scheme tidy would 404 every one of them at once. This repo has already paid that bill - 198 company hubs were deleted and rebuilt by an earlier bug, and the churn suppressed them for weeks."
  },
  "discovery": {
    "_note": "Find postings an employer does not index on its own board, by asking Google Programmable Search. Amazon's India board lists 2,538 roles and four internships, none engineering, while its SDE internship sits at a live URL Amazon's own search will not return - and Google has that page, because it is public, server-rendered and allowed by robots.txt. Runs ONCE A DAY from bin/run.sh: the free tier is 100 queries a day and the scheduler fires 48 times, so a per-scan sweep would spend the quota by lunch for no extra freshness. Needs GOOGLE_CSE_KEY and GOOGLE_CSE_CX in .env; without them it is a silent no-op.",
    "enabled": true,
    "sites": ["amazon.jobs"],
    "_sites_note": "One search per site per query, so sites x queries IS the daily budget. Keep it to boards whose own search is known to be poor - anything with a working board is already read by bin/poll-ats.js far more cheaply.",
    "queries": ["intern India", "internship 2027 India"],
    "_queries_note": "Narrow on purpose. A broad query returns careers landing pages, 'life at' articles and PDFs, and each of those costs a fetch to discover it is not a posting.",
    "dateRestrict": "d7",
    "_dateRestrict_note": "Google's freshness window: d7 = the last week. An internship is worth finding in its first week; without a limit every sweep returns the same aged pages and spends its budget re-checking them.",
    "maxPerRun": 20,
    "_maxPerRun_note": "How many NEW links are resolved per sweep. Already-seen URLs are remembered in the discovered_urls table and never re-fetched, so this only bites on a first run or a sudden burst."
  },
  "publish": {
    "enabled": true,
    "autoPush": true,
    "_autoPush_note": "After each run, commit web/public/data/jobs.json and push. Vercel is watching the repo, so the push is what makes the public site update.",
    "maxAgeDays": 30,
    "_maxAgeDays_note": "How long a job stays on the public site after it was first seen. Raised 14 -> 30 on 24 Aug against measured data, not taste. ATS rows carry ground-truth liveness (they are dropped when they leave the board, see recentJobs) so this window only ever governed LINKEDIN rows, which have none - only 4% of them are ever re-seen a day later, because the search is time-windowed and a card falls out of it almost immediately. Measuring how long ATS internships actually stay open gives median 18 days and p75 30.5, so a 14-day cut was deleting pages for roles STILL ACCEPTING APPLICATIONS 56% of the time - and deleting them at roughly the age they would start to rank. 30 days covers three postings in four for their whole open life. It is a proxy and it will sometimes serve a closed role; the cost of that is mild, because a LinkedIn apply_url lands on LinkedIn's own 'no longer accepting applications' page, whereas the cost of deleting an open one is the whole page. Google requires an EXPIRED posting to stop being served; it sets no maximum age.",
    "_regions_publish_note": "Which regions get a published board is set in the top-level 'regions' block, not here. It replaced publish.indiaOnly, which was a boolean on a site that only ever had one board.",
    "techRolesOnly": true,
    "_techRolesOnly_note": "Only postings the classifier called technical reach the site. Applied at publish time, not in SQL, so rows stored back when the site carried every role simply stop appearing - nothing is deleted and flipping this to false brings them all back. A row whose verdict has not run yet (is_tech NULL) is NOT published: unclassified must not be treated as maybe-technical.",
    "includeFullDescription": false,
    "_includeFullDescription_note": "Leave false. Full descriptions are the posting company's copyrighted text; the site shows your generated summary and links to the original instead."
  },
  "safety": {
    "pauseOnChallenge": false,
    "_pauseOnChallenge_note": "TEMPORARILY OFF. true = a detected security check pauses the run, alerts you, and waits captchaWaitMinutes for a human. It was firing on pages with no CAPTCHA on them: the detector matches page TEXT as well as frames, and the challenge markers - unlike the logged-out ones - are not suppressed when the signed-in navigation is visible, so an ordinary posting containing a phrase like 'security verification' stopped a healthy run. Each false pause also held the Brave profile for 4 minutes, which is what made the FOLLOWING run die with 'Brave would not launch after 3 attempts'. With this off the run screenshots the page (screenshots/challenge-ignored-*.png) and carries on. Turn it back on once the detector is fixed - the screenshots are the evidence for that work.",
    "captchaWaitMinutes": 4,
    "_captchaWaitMinutes_note": "How long to wait for a human to solve a security check. Was hardcoded at 12 minutes with no knowledge of the run budget, which is how a run reached 59 minutes on a 15-minute schedule - it blew past maxRuntimeMinutes, its lock aged out and was treated as stale, and the next run started while this Brave still owned the profile and died on a launch timeout. Also capped at the run's remaining budget, so this is a ceiling and not a promise.",
    "cooldownHoursAfterRateLimit": 24,
    "_cooldownHoursAfterRateLimit_note": "If LinkedIn rate-limits or restricts the session, the tool refuses to run again for this many hours instead of walking back into it. This is the one setting here with real account risk - repeatedly walking back into a rate limit is how a session gets restricted. It was temporarily 0 while the run failures were being diagnosed; restored to 24 on 11 Aug 2026 after LinkedIn invalidated the session outright."
  },
  "summarizer": {
    "mode": "offline",
    "_mode_note": "'offline' = regex/extractive summary, no API key, free. 'claude' = better summaries via the Claude API; set ANTHROPIC_API_KEY in your environment and change this to 'claude'.",
    "model": "claude-haiku-4-5-20251001"
  },
  "ats": {
    "_regions_collect_note": "Which regions are collected is set in the top-level 'regions' block. It replaced ats.indiaOnly. An ATS board is global by nature - one Greenhouse board carries every office a company has - so collecting every region costs exactly the same number of requests as collecting one, while refusing them threw away 176 of the 189 live engineering internships these boards already carry.",
    "maxPostingAgeDays": 120,
    "_maxPostingAgeDays_note": "Raised from 30 to 60 because a first-party board dates a posting by when the REQUISITION was created, not when it was last advertised. Microsoft's API reports 'Applied Sciences INTERN' as 36 days old while LinkedIn was actively promoting it as 4 days old; at 30 we dropped it, along with Software Engineering INTERN (35d) and Data Science INTERN (36d). 60 admits those and still refuses the genuinely dead - Research Sciences INTERN reports 159 days. How old an ATS posting may be and still be collected. The LinkedIn scan never needed this because it searches with a time window; an ATS board has none and lists whatever is still open, so the first real poll surfaced roles 159 and 214 days old. Being first to a seven-month-old listing is not being early.",
    "_note": "Applicant-tracking-system job boards - the source LinkedIn copies from. Discovery is cached in the company_ats table by bin/discover-ats.js; bin/poll-ats.js reads it. No browser and no ToS problem: these are the public endpoints a company careers page calls to render itself."
  },
  "enrich": {
    "model": "qwen3:8b",
    "_model_note": "Ollama model tag, not a Gemini id. Must match something in `ollama list` or every call 404s.",
    "perRunLimit": 40,
    "backfillPerRun": 6,
    "_backfillPerRun_note": "How many jobs stored from card data alone get their page opened at the end of a run to fetch the description we skipped. Confidently non-tech titles are listed without being opened, which leaves description NULL - and needingEnrichment requires a description, so without this pass those rows never gain bullets, stipend or duration. Kept small and time-gated: it only uses slack left after the scan.",
    "_perRunLimit_note": "How many freshly scraped postings get sent to Gemini for bullets, eligibility and skills at the end of each run. The free tier has a daily request cap, and with runs every 30 minutes an uncapped run could spend the whole day's quota in one go. Anything skipped is picked up by the next run, because only postings with no bullets yet are ever selected.",
    "useModel": true,
    "budgetMinutes": 9,
    "_budgetMinutes_note": "Wall-clock ceiling for the whole enrichment pass. Whatever is not done is picked up next run, because only postings without bullets are ever selected."
  },
  "postQueue": {
    "_note": "The hand-picked LinkedIn post queue. An 'Add to post queue' button on each listing in the run report, then one Generate press that asks the local model for an opening line and an applying tip per posting and writes a page of finished posts to copy. NOTHING IS EVER PUBLISHED - the last step is his own hands on his own account, which is the whole reason the queue is manual.",
    "enabled": true,
    "port": 4322,
    "_port_note": "bin/queue-server.js binds this on 127.0.0.1 only. It serves the run report as well, so the buttons in it are same-origin; there are no CORS headers anywhere, which is what stops another site talking to it. 4321 is taken by web/serve.js.",
    "model": "qwen3:14b",
    "_model_note": "Ollama tag. 14b rather than the 8b the enricher uses, deliberately: the enricher runs inside a scan on a wall-clock budget and 14b measured slower AND less accurate at EXTRACTING fields, while this call is asked to WRITE and nobody is waiting on a listing. Drop to qwen3:8b if a batch feels slow.",
    "timeoutSeconds": 180,
    "utm": {
      "enabled": true,
      "source": "linkedin",
      "medium": "social",
      "_note": "Appended to interndoor.com links in a post so Vercel Analytics can separate LinkedIn traffic from organic. NEVER added to an ATS or LinkedIn apply URL - that URL belongs to somebody else and some ATS routers read the query string. Safe against the canonical: every generated page emits <link rel=\"canonical\"> pointing at the clean URL, so a tagged link cannot become a second indexed copy."
    },
    "weekly": {
      "enabled": true,
      "region": "IN",
      "weekday": 0,
      "hour": 10,
      "_note": "The Sunday roundup: one post listing everything the board picked up that week. weekday is 0=Sunday. Generated by bin/weekly.js, which bin/run.sh calls after every scan - the first run at or after `hour` local time on `weekday` writes it, opens it and marks the week done in settings, so there is no second scheduler to install and a Mac that was asleep at 10:00 still gets it when it wakes.",
      "_weekday_note": "Any day works. It is Sunday because that is when someone plans the week's applications."
    }
  },
  "ollama": {
    "_note": "Local model via Ollama, on this Mac. Replaces Gemini for the scan and the enricher; the resume tailor on Vercel still uses Gemini because Vercel cannot reach this machine. No quota, so postings are sent whole and one at a time rather than truncated and batched.",
    "host": "http://127.0.0.1:11434",
    "model": "qwen3:8b",
    "_model_note": "qwen3:8b with thinking disabled. 14b was measured slower (24s vs 9s per posting) AND less accurate on eligibility, so bigger is not better here.",
    "timeoutSeconds": 120,
    "_timeoutSeconds_note": "Hard ceiling per call. A stuck model must never delay a listing - the run continues and the posting is enriched next time."
  }
}
